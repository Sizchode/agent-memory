# Mem0's default OSS configuration uses Qdrant, already included at runtime.
-r ../runtime.txt
torch>=2.8,<2.9
transformers>=5.16,<5.17
sentence-transformers>=5.2,<5.3
spacy>=3.7,<4
en-core-web-sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl
fastembed>=0.3.1
# The official benchmark uses Mem0's default Qdrant store with its fastembed
# BM25 encoder plus spaCy lemmatization/entity extraction. Package dependencies
# are otherwise resolved from the checked-out Mem0 pyproject.
