# 研究计划

更新：2026-09-20。主方法、已有消融和成绩分别保留在 [novelty.md](novelty.md) 与 [results.md](results.md)。本页区分当前计划与历史探索，不把尚未通过实验的候选当成主方法。

## 当前目标与执行顺序

**2026-09-20 最新用户目标：** 保留四个 reader，one-shot QA 使用同一个方法配置，力争每个 reader 都至少在 5/6 任务超过完整九项 baseline 的逐任务最佳；这取代此前“至少三个 reader 多数胜出”的验收口径。优先补 Llama，但不允许按 reader 或数据集选择不同方法。IRCoT 同时补齐上下文增强，保留统一 1/3/5 轮曲线和同协议 BM25 对照。先优化完整 pipeline，稳定后与用户回顾，再设计系统性消融；开发只保留判断新增操作是否有用的必要原版对照，不继续展开大规模消融组合。目标不是分数保证，不能借改指标、删题或挑轮数把未达到写成达到。

**最新执行状态：单次 QA 的四 reader 目标已达到。** 保留原文、以十条问题相关事实替换全量附录的同一配置，对各自同环境九项原生 baseline 的严格胜出数为 Qwen4 5/6、Qwen9 6/6、Gemma 5/6、Llama 5/6。另外三个 reader 的迁移复用原先在 Llama 验证的同一份上下文，不重新选择事实；全部原生对照及汇总均已完成。相对旧完整方法，四个 reader 的 LoCoMo 都有下降。Llama 同组件 BM25/HippoRAG 2 对照仍有两项高于我们，不写成所有配置都胜出。继续冻结此 one-shot 配置，不因已达标而停止多轮验证，也不将它自动称为已成立的构图贡献。

Qwen9 全量 QA `6540929`、原生对照 `6540673` 和比较汇总 `6540934` 均已完成。普通查询模块为 `optimization/retriever/query_fact_context.py`，旧入口默认方法暂不变；修正 recognition 缓存路径后，CPU 预检 `6541003` 和全量重放 `6541021` 都已通过。六任务全部 3386 题、15 组的检索及回答上下文与获胜缓存完全相同，零新增编码或识别调用。失败记录、代码归档和完整结果保留；重放、原 one-shot 组件试验、原生对照及 reader 迁移的已完成脚本核对归档后移除。

固定轨迹的 IRCoT 事实补充 CPU 预检 `6541026`、全量准备 `6541194` 和四组 GPU 预检 `6541029` / `6541031` / `6541036` / `6541037` 已全部成功。Llama/Qwen4/Qwen9/Gemma 的全量 QA 分别为 `6541203` / `6541210` / `6541220` / `6541221`，各 10158 条、三档轮数和六任务完整，合计新增 40632 条 QA。直接复用已完成的同环境原版对照，实际 token 超长则报错，不删除问题或悄悄截断。目录 `optimization_ircot_fact_context_seed42_20260920`。Llama 在线增强 `6540627` 仍使用旧完整附录，是独立条件，不能把 one-shot 的新收益记到它名下；不按 reader 切换配置。

四组固定轨迹 IRCoT 全量均已完成。对 BM25，Qwen4 在 1/3/5 轮为 6/6/6 胜，Qwen9 为 6/5/4 胜（五轮另平一项），Llama 为 6/5/5 胜，Gemma 为 6/3/3 胜（后两档各另平一项）。合计 61 胜 3 平 8 负，完整曲线已生成，全部保留；不能把胜出条件数叫作问答准确率。三轮 QA 输入约为图原文的 2.4 到 2.6 倍，不能不计上下文代价。当前配置同时固定用于 one-shot 和 IRCoT 最终 QA，不根据 reader 结果切换开关。

独立的旧完整附录在线增强 `6540627` 已成功结束，用时 7:01:33，六任务全量、三档共 10158 条 QA。Llama 对 BM25 的 1/3/5 轮为 4/3/4 胜，不替换主方法。三轮在线 SH/MH/FC-SH/FC-MH/LoCoMo/2Wiki 分别为 86/61/17/0/50.89/40.87；主方法同档为 87/64/40/9/49.93/52.46。在线分支有 LoCoMo 收益，但不是普遍改善，详表和成本见 results.md。用户认可当前结果已基本到位；停止新的刷分与模块扩展，保留冻结配置、完整正负结果和缓存，进入下面的消融设计讨论。没有自动启动消融。

### 主实验后的消融草案（待回顾，不自动开跑）

本轮优化收尾核对：四模型的主表均含九项原生 baseline、全部六任务，题数固定为 100/100/100/100/1986/1000；严格胜出为 5/6、6/6、5/6、5/6。四模型 IRCoT 的 1/3/5 轮和独立 Llama 在线对照均已完成；普通接口的 3386 题、15 组重放与获胜缓存完全一致。结果、负分支、成本、代码归档、原图及 peer share 保留，全部实验及服务作业已结束。以下是下一阶段的消融设计，不表示这些消融已经完成。独立数据只完成候选、历史使用和格式兼容性核对，未确认未开发数据的独立性，也未运行独立泛化评测。

主表已齐，先与用户回顾固定配置及其适用边界，再运行系统性消融；不继续按数据集刷参数，也不为了 Gemma 单独添加路由器。当前需要检验的是模块的作用，而不是把每个通用设置都宣称为理论最优。

| 问题 | 对照如何做 | 当前证据 |
|---|---|---|
| 收益是否依赖改过的图与候选索引？ | 换回原 HippoRAG 图与索引，保留相同融合、事实补充和原文展示 | 原生 HippoRAG 主表不能代替这个控制；先核对已有图/索引消融能否在新协议复用 |
| 关键词检索融合是否必要？ | 只撤销 BM25 融合，其他模块固定 | 旧批次的纯图结果保留，但不能直接当作新上下文配置的消融 |
| 问题相关事实是否优于完整附录或无附录？ | 固定检索结果与相邻原文，只更换事实附录 | 完整附录控制已齐，可复用；原文加窗口但无事实附录仍需单独验证 |
| 相邻原文是否必要？ | 删除窗口展示时保持已选事实不变，单独测展示作用；若移除整个窗口机制，要明确事实候选范围也随之改变 | 旧版删窗口结果不能直接外推到当前相关事实配置 |

控制范围沿用四 reader、六任务全量及 IRCoT 三档，不更改任务提示和评分。只改最终上下文时复用轨迹；改检索模块时必须重新运行 IRCoT，不能拿旧轨迹冒充新检索器效果。各对照同时报告输入/输出 tokens，不能将不同文本长度的差异全部归于结构。若论文进一步声称图连接与候选筛选分别有效，再使用二者独立开关的四格控制；不先铺开新的数值搜索。冻结图、抽取与向量继续复用，所需生成调用及费用如实记录。

### 既有探索与执行记录

已排除的候选保留：Qwen 重排、默认 LLMLingua-2 压缩及十事实替代原文均未采用。四 reader 的最终 QA 增强全部完成，共 121896 条 QA；对 BM25，Qwen4/Qwen9/Gemma/Llama 的严格胜出数分别为 6/4/4、6/5/5、6/5/5、4/2/2（轮数 1/3/5），不按 reader 选开关，统一增强未采用。两个用完的执行脚本已核对归档后删除。下方旧提交状态按历史记录保留，最新成绩见 [results.md](results.md)。

固定对照也需要完成当前环境的验证：Llama 九项原生 baseline 已齐，新增 Qwen4/Qwen9/Gemma 的同批 vLLM 预检 `6540655` / `6540656` / `6540657`。每个 reader 预检十种方法、300 条 QA；通过后单独跑九 baseline 加旧完整方法各 3386 题，不重新检索、不改 prompt 或参数。这样后续方法比较不用再引用不同推理批次的旧分数；也不预定旧 6/6 在新批次仍成立。输出为 `optimization_native_readers_seed42_20260920`，Llama 已完成的同环境结果只读保留，不重复生成。

上述三项预检均已通过，全量分别为 Qwen4 `6540672`、Qwen9 `6540673`、Gemma `6540691`。每项 33860 条 QA、八小时作业上限，未增加方法配置；全量输出尚未完成，不能预报分数。

在线增强预检（Llama `6540321`）已完整通过，包括六任务各首组两题、三档 QA 及原控制器重放；单独提交 Llama 全量 `6540627`。沿用 [IRCoT](https://aclanthology.org/2023.acl-long.557/) 原控制器、停止规则及旧图/BM25 融合检索。检索、去重、600 词过滤仍以原文身份执行；显示阶段展开旧冻结上下文，不受原来的 350 词段落截断。必须区分第二层限制：原 `GenerationSlot` 仍按 GPT-2 tokenizer 的 8000-token 规则移除示例，必要时从提示开头删行，预检已观察到该行为。因此不是完整上下文无损进入每轮推理，也不声称等 token 成本。最终 QA 则使用原生任务提示及实际 reader 的 32768 上限。没有为我们单独调大原生推理提示上限，也没有删除问题。新子问题允许 embedding/recognition，不重做 OpenIE、不改图。输出为 `optimization_ircot_online_context_seed42_20260920`；尚无全量效果结论。

十事实替代原文的 Llama 全量已完成但不采用：对九 baseline 六项皆负，FC-SH 从原完整方法 32 降到 2；不扩展四 reader。下一项保留原文及既有来源窗口，只把全量事实附录替换成已选的十条问题相关事实。直接复用刚完成的选择缓存，不再筛选新参数；BM25/HippoRAG 2 同样保留原文并补其选择结果。新目录为 `optimization_fact_context_seed42_20260920/source_and_fact_context`，预检 CPU/GPU 为 `6540619` / `6540621`。这是事实补充方式的检验，不能将旧负结果删掉或改名成新结果。

下一项最小检验借用 [KAPING（ACL 2023 Workshop）](https://aclanthology.org/2023.nlrse-1.7/) 的相关事实选择组件。原论文做 WebQSP/Mintaka 的知识图问答，主实验使用数据标注的问题实体取一跳事实，另报告自动实体链接对照；再用 MPNet 相似度选十条事实，较相关事实靠近问题。本实验不使用标注实体，也不复现它的实体链接、语料或 KGQA 指标，只采用十事实上限与相似度选择，候选来自当前已检索来源，向量复用已验证的 Qwen3-Embedding-0.6B 缓存。仍使用本项目六任务全量、原生 QA 与评分，不创建新切分或调整任务参数。既有三元组可能丢失限定条件和原文内部序号，故不能预设它优于完整原文。

具体条件为 BM25、HippoRAG 2、我们各自来源内的事实选择；原生九 baseline 的完整流程保留作主比较。图和来源检索不动，我们只使用原图保留的事实，两个辅助对照使用原 OpenIE 事实；这属于上下文组织试验，不称为新的图结构。缓存缺失报错，不补新抽取或编码；候选为空时保留该题与空上下文，不增加任务特例。筛选耗时和 QA tokens 单列，原来源检索耗时只读沿用。先运行六任务 15 组各两题的 Llama 接口预检，再决定全量；统一配置不根据 pilot 分数改动。目录 `optimization_fact_context_seed42_20260920`，CPU 预检修复目录创建后为 `6540267`，依赖它成功的 GPU 预检为 `6540268`。

上下文增强首先检查最终 QA，随后再决定每轮推理也使用增强文本的在线实验。二者不能混报。新预检直接复用旧完整方法的冻结原文、事实附录和相邻上下文，不新增摘要、窗口参数或模型。目录 `optimization_ircot_context_seed42_20260920`，条件为 BM25 原文、图融合原文、图融合增强上下文，全部使用既有 1/3/5 轮轨迹。Llama、Qwen9B、Qwen4B、Gemma 预检依次为 `6538388`、`6538389`、`6538390`、`6538391`，各 `gpu-debug`/L40S、单 CPU、48 GB、55 分钟；QOS 总内存限额下最多两项同时运行。所有条件在同一次 reader 环境重新 QA；先检查实际 token 长度，超长报错，不裁剪原文或移除问题。尚未取得该增强在 IRCoT 上的效果结论。

持续目标已开启。研究问题是：**不训练新模型，能否利用已有记忆之间的关系，根据当前问题和已经取得的证据，选择更有助于回答的上下文？** 图是实现手段；推荐系统提供可考察的已有方法，不预先作为贡献名称。现有 PPR 已有查询相关的起点，不能仅改称“推荐”就算新算法。

上下文预检进展：Llama 与 Qwen9B 的 `complete.json` 均已确认完成，包含三个条件和全部 1/3/5 轮的接口 QA。按原设置提交全量 `6538590`（Llama）与 `6538593`（Qwen9B），各为 `gpu-he`/L40S、单 CPU、48 GB、4 小时上限；每个 reader 共 30474 条 QA。全量会先检查全部输入长度，不根据预检分数选择设置。Qwen4B/Gemma 仍等待各自预检完成后再进入全量。此时尚无全量增强效果结论。

随后 Qwen4B 与 Gemma 的完整预检也通过，按同一设置提交全量 `6538609` / `6538610`。四 reader 的预检都已通过，四组全量均已提交。在线接入前需要保留原始出处身份：当前官方 IRCoT participant 会丢弃超过 600 词的段落，并在推理提示中默认仅展示每段前 350 词；直接替换检索原文会同时改变这些行为和去重身份，不能当作只增加上下文。最终 QA 增强实验不改这些中间检索行为。

**当前起点是保留旧完整方法，不再要求推翻重做。** 固定 `statement_projection_loop_free_retained_index_rrf_window` 的构图、索引与参数，继续改善 one-shot 与 IRCoT，优先检查 Llama。旧版 one-shot 相对九项 baseline 为 Qwen4B 6/6、Qwen9B 6/6、Gemma 5/6、Llama 3/6 严格领先；IRCoT 中旧图加 BM25 在统一三轮时为 19 胜、2 平、3 负（四模型乘六任务）。这是本地开发结果，不是全领域 SOTA，也不证明改图单独造成全部收益。未融合版、融合版、单次完整上下文的结果必须分开。

最新完整检验改变了下一步选择：Llama 单次 QA `6537744` 的 44018 条预测和完整比较已完成。对本次九项原生 baseline 最佳，原排序为 2 胜 1 平 3 负，新增 Qwen 重排为 3 胜 3 负；重排相对自身原排序仅 MH 上升，其余为一平四负。不将该重排升级为统一 one-shot 主方法，不据单项提升扩展四 reader。历史旧版 3 胜 1 平 2 负与本次推理批次分开报告，不能拼接。下一步继续已提交的固定默认压缩候选和 IRCoT 上下文增强；IRCoT 重排等其自身完整结果后再判断。

IRCoT 重排随后也完成：`6537747` 的 40632 条 QA 全部通过。图重排对原生 BM25 在 1/3/5 轮分别为 6/6、5/6、5/6 严格领先，但对图自身原排序为 6 胜 3 平 9 负，2Wiki 三档均下降；同样重排 BM25 后，图的严格胜出数为 5/6、4/6、4/6。保留全部曲线，不以一轮 6/6 宣称解决了多轮差距，也不升级为统一主方法。该探索的三个临时脚本已验证归档后删除，原始缓存与结果保留。当前候选仍为已启动的上下文增强与压缩，不继续增加重排权重或按任务开关。

当前最小实验是**固定证据，只比较原排序与 Qwen3-Reranker-0.6B 官方相关性排序**。不新增权重、去重规则、任务路由或训练，不把检索历史当正反馈。这不是推荐系统新算法，也尚未使用跨轮证据条件；先判断已有相关性模型对 Llama 的上下文顺序是否有可复现收益。one-shot 保留九项 baseline 与旧完整方法的原生排序，仅 BM25、HippoRAG 2、我们另做同一 reranker 的补充控制，共 13 个条件；IRCoT 对官方 BM25 与旧图融合后端运行原排序/重排，固定 1/3/5 轮。每个条件都在同批 vLLM 上重新 QA，避免把 Transformers 历史成绩当作本轮配对对照。原生主比较、我们自身的新增模块消融、同组件补充控制分开报告。IRCoT 此步固定中间轨迹，不能写成在线检索已改善。

独立目录为 `optimization_context_reranking_seed42_20260920`。原 one-shot 预检 `6537412` 已取消，改为覆盖九项 baseline 的 `6537471`；IRCoT 预检为 `6537413`。两项 `gpu-debug`/L40S 预检均已成功（7 分 48 秒、7 分 29 秒），每条件包含原六任务 15 个来源组各两题，共完成 960 条 QA；不按小样本分数调参。选择性重排控制的语法与条件检查已通过。全量分别为 `6537744`（one-shot，13 条件、44018 条 QA，`gpu`/L40S）与 `6537747`（IRCoT 固定轨迹，12 条件、40632 条 QA，`gpu-he`/L40S），各单 CPU、48 GB、4 小时上限。两项均已开始准备评分，尚无全量效果结论。完整正负结果出来后再决定是否扩展四 reader 和在线 IRCoT。原抽取、图、检索结果与推理轨迹只读复用；一次性程序置于实验目录，运行代码 ZIP 随结果保存。

**公平比较约束（用户进一步澄清）：** 主比较保留各 baseline 的完整原生算法，包括其已有筛选、排序和上下文模块，不强制替换或再叠加同一个 reranker。我们的新增重排也须显式命名并报告成本及自身消融，不能将其收益全部归因于构图。统一外接 reranker 是补充组件对照，不是公平主实验的必要条件。已核对当前原生调用路径：HippoRAG 2/CatRAG 有三元组筛选，AnchorMem 的同名函数为向量 top-k，不能据函数名认定它们已有同类段落重排。全量只扩展 BM25/HippoRAG 2/我们三项重排控制，不额外重排其余七项原生方法；没有移除它们原来的筛选或排序。

对于统一重排补充对照，模型、版本、官方指令、8192-token 评分上限及官方截断方式相同；截断只作用于重排模型输入，QA 原文不变，报告被截断的评分对数。每个方法的原排序与重排保持证据集合、条数、QA 提示和生成设置不变。各 baseline 原有记忆表示、上下文长度与预算并不相同，仍需报告，不能声称等 token 成本。

**验收口径以页首最新目标为准：one-shot 四个 reader 各至少 5/6 严格领先。** 单次 QA 对照完整九项 baseline，多轮对照同协议 IRCoT + BM25，报告固定 1/3/5 轮曲线及全部六任务。第四个 reader 的回退不再作为自动接受的终点；仍不按 reader 或任务另选主算法，不将平局计为严格领先。

先核对相关论文的任务、方法、原生指标和数据划分，再选择可直接检验的 training-free 方法。不训练 router/ranker，不按数据集名称切换配置，不新增手写评分组合。使用冻结的预训练模型不等于训练新模型，但其调用与成本仍须报告。

执行顺序：

1. 收完已启动 Llama 单次 QA 与固定轨迹重排，保留原生 baseline；新启动四 reader 的 IRCoT 旧上下文增强预检，复用冻结图和上下文，不重复抽取。
2. 根据完整六任务结果决定哪些已有操作值得保留，再核对推荐系统等相关已发表方法及兼容条件；不能仅因为名称好讲故事就增加模块，也不能把已检索到的证据视为正反馈。
3. 对候选完整流程逐项预检并验证四 reader、六任务与 IRCoT 固定 1/3/5 轮；最终 QA 增强不能冒充在线迭代改进。稳定后再与用户回顾并设计系统性消融。

新增候选预检：使用 [LLMLingua-2（Findings ACL 2024）](https://aclanthology.org/2024.findings-acl.57/) 已发布且本地缓存的 mBERT 压缩模型，仅做推理，不在本项目训练。原论文以 token 分类做任务无关压缩，评测包括 MeetingBank、LongBench、ZeroScrolls、GSM8K、BBH，不是我们的六任务复现。直接调用安装库的 `compress_prompt_llmlingua2` 默认值：`rate=0.5`，不强制保留指定词或数字，不按任务调整比例，也不加入新的文档筛选规则。保留原检索条数、顺序、QA 模板及原生评分，只改变证据文本；压缩后文本的精确匹配检索指标不能解释为召回变化。已有“去掉证据上下文”消融在 Llama 的 FC-SH 为 47、FC-MH 为 4、2Wiki 为 51.10，但 SH 和 LoCoMo 退步；该消融同时移除融合、窗口和事实附录，不能直接证明仅文本冗余造成失败。新预检是检验压缩是否值得全量验证，不据此预定采用。

该候选目录为 `optimization_context_compression_seed42_20260920`，预检 `6538983` 在 `gpu-debug`/L40S、单 CPU、48 GB、55 分钟内运行。三种方法为 BM25、HippoRAG 2、我们，各保留原文与压缩两个条件；每条件六任务 15 组各两题，共 180 条 QA。全量题目身份先核对，再取接口样例，不按样例分数调参数。全部压缩输入仅含证据文本，不向压缩器传问题或答案，压缩缓存以原文精确匹配复用。该预检不取代九项原生 baseline 主比较，也不声称构图新贡献；通过后才能决定全量执行。压缩模型版本为 `5f0c82792b7ea14c6484e015b6a072009496b7f2`。

压缩预检随后成功：390 段不同证据、180 条 QA 完整，实际压缩环境为 LLMLingua 0.2.2、Transformers 4.57.0、Torch 2.8.0+cu128。按同一默认设置提交全量 `6539257`，依赖 Llama 单次 QA `6537744` 成功，使用 `gpu`/L40S、单 CPU、48 GB、4 小时。复用预检压缩缓存；原文 QA 只在 reader 元数据、原生评测完成标记和逐任务序列化输入完全一致时复用，原文件只读。新增三种方法各 3386 条压缩 QA，共 10158 条，同时读取已完成九项原生 baseline 的完整同环境比较，不重新生成原文对照。若任一复用检查失败则报错，不混用旧环境成绩。新增的复用检查尚待全量运行实际验证。

该压缩全量随后完成并被否决：复用检查全部通过，我们对原生九 baseline 为 1 胜 1 平 4 负，对自身原文为 2 胜 4 负。虽然 2Wiki 从 43.94 升到 46.45，LoCoMo 从 49.57 降到 42.08，不能按任务开关拼出新主方法，不扩展到另外三个 reader。Gemma 的 IRCoT 最终上下文增强也已完整结束，1/3/5 轮对 BM25 分别赢 6/6、5/6、5/6，但三轮 2Wiki 比图原文下降 6.99 点，QA 输入明显增加。继续收完另外三个 reader，再决定统一上下文配置；不据一个 reader 的部分指标启动更多组件组合。

用户已授权使用 `gpu-he`、`gpu`、`gpu-debug`。2026-09-19 核对的 GPU 用户配额分别为 6、2、4 张，实际并发仍受 CPU、内存、空闲卡和既有服务占用限制，不代表保证同时获得 12 张卡。`gpu-debug` 的 QOS 限时一小时且总内存配额 96 GB，仅安排预计能在 55 分钟内完成的预检或独立短作业；长作业放其他两分区。优先已有 vLLM 批量生成、完成条件跳过和共享只读缓存；不同作业不得同时写同一结果目录。不同卡型先验证环境与模型路径，不为抢卡改变模型精度或评分协议。CPU 分析放 `batch`，不在登录节点执行全量扫描。

执行优化检查发现，当前 HippoRAG 仅在进程内复用查询向量，CPU 检索作业仍逐题执行首次编码。单独的 GPU 批量查询缓存准备 `6533630` 已通过全量验证：六任务、15 组、3386 题，清空内存查询表后重新加载文件，禁止再次编码，原 HippoRAG 证据和顺序全部重放一致，零 recognition 缓存缺失。沿用原编码函数、模型、归一化及输入，缓存加载检查编码器版本与设置。累计批量编码 5.78 秒不含模型与索引加载，重放检索 302.05 秒，不将这些分项计时冒充端到端加速比。此缓存供后续实验显式选择使用，当前 CPU 作业不变，也不假设 GPU/CPU 向量逐位相同；新生成的 IRCoT 子问题不在这份单次查询缓存内。

最新已完成结果：四 reader 的 gold-only 三条件对照共 12000 条预测；Gemma 的事实筛选消融五条件各 3386 题。删除筛选使事实图 FC-SH 从 48 提高到 58，但 MH-Doc QA 从 53 降到 46、2Wiki F1 从 50.30 降到 46.64，不是无损简化。完整数字见 [results.md](results.md)。下方作业提交与失败记录保留为历史过程，不能用旧状态判断当前是否完成。

### 已完成并否决：选过证据之后改变排序

采用 [GRASSHOPPER（NAACL 2007）](https://aclanthology.org/N07-1013/) 第 2 节的吸收随机游走作为已有方法对照：第一条仍按原 PPR 选择，将已选条目设为吸收状态，后续条目按吸收前的期望访问次数选择。期望次数按原文 Eq. 4 在所有未吸收节点上均匀平均，不新增相关性与多样性的手写加权分数。参数沿用当前 damping=0.5，与原论文摘要实验选用值相同，不在本项目逐任务调参。

本项目的适配边界是：图包含实体、事实与原文，只有原文节点可作为输出及吸收状态；其他节点继续参与传播。这不是作者的完整摘要任务复现。原论文使用句子图、位置先验及 ROUGE；本项目固定现有知识图、查询起点及各任务原生 QA 指标。标准算子不能充当新贡献；减少相似材料也不等于补齐多跳证据，可能反而移除必要的相关材料。

先固定五个条件：BM25、HippoRAG 2/PPR、HippoRAG 2/吸收排序、事实图/PPR、事实图/吸收排序。每个图内只切换排序，逐题验证筛选候选与 PPR 起点完全相同、第一条输出不变；带原 PPR 的两个条件必须精确重建已有原文及顺序。原图与事实图之间仍包含既有起点变化，不解释为纯拓扑对照。空筛选仍按原流程退回 dense，缓存缺失报错，不调用新 generator。

实现使用 SciPy 稀疏线性求解，先与论文的稠密矩阵公式核对，包括非均匀先验、孤立节点、自环及仅原文可选的情况。数值容差仅用于求解检查，不是任务超参数。输出记录求解残差、耗时和各步访问次数；不同步骤的次数不当作可横向比较的相关性概率。

六任务、15 个来源组各前两题共 30 题仅用于接口预检，分别用 Gemma、Qwen4B 验证 QA；通过后保持同样设置跑六任务全量，不根据预检分数调权重。当前仍是已有方法的组件实验，尚无涨分结论，也尚未实现跨 IRCoT 轮次继承吸收状态。实验目录：`optimization_absorbing_ranking_seed42_20260919`。暂不改主方法默认配置及 peer share。

执行链已经提交：CPU 预检 `6532329` -> 两项 GPU QA 预检 `6532330` / `6532331` -> 六任务各一个独立 CPU 全量作业 `6532522` 至 `6532527` -> Gemma / Qwen4B 全量 QA `6532539` / `6532540` -> CPU 汇总 `6532670`。各阶段依赖前置检查成功，不以预检分数作筛选门槛。没有新建多份 launcher。预检现已全部完成：三项数学检查、30 题及 60 次旧检索重放通过，零 recognition 缓存缺失，两个 reader 的五条件 QA 完整。小样本涨跌不一致，保留原设置进入六任务全量；完整数字见 `results.md`。

执行层随后改为流水安排：已完成检索的五项任务先由 `6533725` / `6533726` 运行 QA；原两个全量 QA 作业各自等待对应作业与 2Wiki 完成后补齐，已完成条件不重算。同一 reader 不并发写入，最终报告仍严格要求六任务全部齐全，不改变算法或缩减评测范围。

两个 reader 六任务全部完成，汇总 `6532670` 逐题核对通过。吸收排序相对事实图原排序在 Gemma 为 4 胜 1 平 1 负，Qwen4B 为 1 胜 1 平 4 负。2Wiki F1 分别从 50.30 降至 48.89、57.89 降至 57.26，段落 Recall@5 从 81.80% 降至 81.175%。对先前九 baseline 的逐任务最佳分别仅赢 3/6、2/6，不扩大至其余 reader 或 IRCoT，不采用为新方法。减少排序冗余并不自动等于保留必要的支持证据，不据此追加按任务开关。代码与测试归档后已删除，缓存及完整负结果保留。

独立验证准备（只读核对，未启动实验）：本地 HippoRAG 发布的 `musique.json` 有 1000 问题，`musique_corpus.json` 有 11656 段落、9838 个不同标题。它是 HippoRAG 的已发布评测样本，不能直接称为 MuSiQue 官方完整 dev 集。当前冻结的 `final_qwen3_30b_seed42_clean_20260910/hipporag2` 根目录只有六个约定任务，没有 MuSiQue 任务产物；但 2026-09-07 的历史记录明确做过 `max_queries=1` 的 MuSiQue 加载检查及段落格式测试，因此不能声称从未接触过。此次核对不足以证明整个实验历史都没有用 MuSiQue 结果选择方法。

当前 `dataset_loader/loader.py:173` 明确只接受 2Wiki，并依赖其 `supporting_facts` / `context` 格式；MuSiQue 使用 `paragraphs` 等字段，且语料标题不唯一，不能只放宽任务名称或按标题猜支持段落。[MuSiQue 官方仓库](https://github.com/StonyBrookNLP/musique) 区分 Answerable 与 Full，并发布 train/dev/test 及 `evaluate_v1.0.py`；Answerable 报告 answer F1 和 support F1，Full 还涉及回答充分性。后续应明确采用哪个已发布任务和切分，沿用对应的官方格式、支持段落标注和评分，不把 HippoRAG 样本上的 RAG 成绩写成完整 MuSiQue-Full 成绩。

在完整方法冻结并核对使用记录前，不在该数据上调参，也不启动新抽取或自行创建转换器。需要的官方格式接入另行验证；当前状态是“数据候选和兼容性已核对，独立性尚未确认”。这不取代六任务主实验，不另造切分或筛选容易的题目。

## 1. 目标与报告口径

- 用户再次确认核心是单次 QA 与超过 IRCoT + BM25 的多轮检索。单次仍保留已有九项 baseline；多轮以同协议 IRCoT + BM25 为明确对照，不把证明通用图理论或额外引入 agent 系统当作完成条件。机制解释应服从这两项实验的证据。
- 最新 one-shot 目标为四个 reader 各至少 5/6 任务严格领先，使用同一方法配置，不用跨任务平均分掩盖回退。此前 3/4 reader 的多数胜出目标已被用户更新。
- 单次 QA 对比已有完整九项 baseline 的逐任务最佳成绩，多轮对比同一 reader、同一轮数的官方 BM25 IRCoT，并补齐上下文增强；所有轮数和回退均保留，不能逐任务挑 cap 拼出主结果。
- 四个 reader、六个任务、全部问题均报告。主要曲线为 1、3、5 轮，已有 7 轮结果保留。主表可以事后选定一个统一轮数并明确说明；逐任务最佳轮数另外列为事后选择结果，不能冒充统一设置。比较最佳轮数时，baseline 也允许在相同轮数集合内选择。
- 开发继续使用这些 benchmark，明确披露 test-as-dev、单 seed 和方法选择过程，不称为独立泛化验证或全领域 SOTA。
- 按最新研究约束，不新增手写经验规则、自造评分公式或为结果改动数据处理；先核对相关方法的原协议，再使用其已有组件建立对照。不得按数据集名称、问题 ID、答案或已知成绩走特例。保留过去的探索记录，用消融决定已有模块的去留，而不是将所有调参都改称理论。

## 2. 保留什么，放弃什么

保留原文、实体、原始三元组、出处 ID、已有向量、官方数据加载与评分、批量 QA 和 IRCoT 执行器。先不重新运行全库 OpenIE。旧完整方法保留为主方法及固定参照，全部有效与失败结果保留；当前只检查上下文选择，不扩充手写参数搜索。

此前“不预设沿用 PPR、旧投影、最后出处筛选或 RRF”的重建探索保留为历史。新候选若不能超过完整旧版，不替换主方法；不会只为更换理论名称而放弃已有强结果，也不会把原有经验规则改称定理。

## 3. 先确认失败发生在哪里

使用已完成的 Qwen4B 和 Gemma 六任务轨迹，不新增模型调用：

1. 逐题核对原生 QA 分数，统计图相对 BM25 的胜、平、负，而不仅看任务均值。
2. 在有原生出处标注的任务上，区分 QA 下降时出处召回较低、相同或更高的情况。没有出处标注的任务不自行生成伪标签。
3. 检查增加轮数后的 QA 变化、实际检索问题与证据变化；区分找不到证据与证据已存在但回答错误。
4. 例子按原数据顺序展示，并同时给出全量计数，不用少量案例宣布因果结论。两个检索后端后续生成的问题不同，因此整条轨迹的差异不能直接归因于一次检索。

这些诊断不修改原生评分，不把答案 F1 称为检索 F1，也不把出处齐全等同于答案必然正确。

首轮诊断已经完成：Gemma 在 2Wiki、3 轮时，图检索找齐标注段落的题目为 851/1000，BM25 为 714/1000，但答案 F1 为 48.73 对 51.84。图比分较低的 177 题中，有 140 题已经找齐标注段落。因此，“图没找齐证据”不能解释多数此类失败；接下来优先检查证据选择、排序与原生 QA 输入，而不是直接增加图扩展范围。这是描述性结果，不证明噪声或顺序是唯一原因，不能外推到没有出处标注的五个任务。

### 当前检查：检索是否必须依赖 LLM 筛选

**优先级更新：** 用户所说的 LLM 依赖，实际指“给出正确证据也未必答对，QA 因此不好优化”，不是要求减少生成模块的数量。下面的筛选消融继续作为模块检查，不代替 gold-only reader 诊断，也不把它包装成对该困难的直接解决。

用户指出流程过度依赖 LLM。先固定已有 OpenIE、Qwen3-Embedding-0.6B 和 reader，不扩大重新抽取。分别检查在线事实筛选、多轮查询生成和最终回答，不能将三者一起替换后把收益归于图。固定相同多轮查询序列的回放对照尚未实施；正式 IRCoT 仍按完整控制器运行。

先执行 [HippoRAG 2 第 6.1 节、表 4](https://arxiv.org/html/2502.14802v2#S6.SS1) 已有的去掉 triple filter 消融。在原 HippoRAG 2 和当前无相似边的事实图上，分别保留或删除 LLM 筛选，另列 BM25，共五项。删除方式是直接保留原 embedding 排名前五的候选，不改候选数量、排序、图内传播规则、五条原文输出或原生 QA。两种图之间还包含既有起点规则差异，不将其当作纯拓扑消融。筛选为空时原版会退回 dense；删除筛选也会改变这条分支，必须一起解释。

预检覆盖原 15 组各前两题，共 30 题；全部六任务题目身份与数量先核对，再取接口样例。每题重建两项带筛选的原检索，逐条核对证据顺序和分数，检查开关筛选时候选完全一致；识别缓存缺失立即失败，不调用新 generator。Gemma 五项 QA 使用相同批量 reader 与原生评分。通过后才运行全部六任务 3386 题。均使用单 CPU、一张 L40S，不中断原四模型 IRCoT。

首个预检 `6525392` 在配置解析时失败（3 秒）：目录名 `SH-Doc_QA` 被误传作原任务名 `SH-Doc QA`，未进入检索；依赖全量 `6525399` 自动取消，零运行时间。修复只使用 `optimization.run_graph.TASKS` 中已有的任务名映射，六任务配置解析及筛选开关的保留候选、原调用转发、空输入检查均通过；替代预检为 `6525421`。失败归档与日志保留，不将入口修复当作方法变化。

替代预检的检索部分已完成：30 题、两种图共 60 次旧检索重建一致，开关前候选完全相同，识别缓存缺失为零。QA 预检尚未确认完成；全量依赖作业 `6525517` 只在整个预检成功后执行，失败则自动取消。此处的检索一致性不表示删除筛选后的 QA 无损。

该预检随后完整成功（3 分 26 秒），但全量 `6525517` 在 FC-SH 的内部检索得分核对处失败（1 分 21 秒），尚未进入 QA。五条原文及顺序相同；报错项为 `0.0029396355917982253` 对 `0.002939632403243072`，超过脚本原先设置的数值容差。没有放宽容差后继续称数值一致：新运行另存 `exact_context`，仍严格要求旧控制的原文内容与顺序完全相同，逐条保存全部非完全相同的内部得分。原生 `_answer_prompt` 仅使用原文与顺序、不读取检索得分，故该检查对应本次要固定的 QA 输入；不声称内部得分逐位重建。新预检 `6525598`、依赖全量 `6525600` 保持相同方法、原生评分和资源设置，原失败目录保留。

输出目录为 `optimization_recognition_ablation_seed42_20260919`。这是依赖性消融，不是新算法，也不能将删除在线筛选称为整个流程不使用 LLM。结果未出之前，不决定把它用于其他 reader 或完整 IRCoT，也不根据样例分数调参。

固定查询回放先做基础核对：CPU 作业 `6525576` 使用 Gemma 已保存的 BM25 轨迹，通过原 IRCoT 控制器回放全部六任务、3386 题，逐题检查 1、3、5 轮的查询文字、请求条数、停止位置及最终证据。复用原推理文本，不新增 LLM 调用或评分；须先核对全部 15 组轨迹齐全，再开始全量回放。输出为 `optimization_ircot_fixed_query_replay_seed42_20260919`。当前只是相同检索结果下的重建检查，还没有替换检索器，因此不构成不同图的效果比较。

首个 CPU 回放在 35 秒后失败：全部任务与组的轨迹存在性检查已通过，但初始化官方提示时 launcher 缺少 `HF_HOME`，离线 tokenizer 找不到现有缓存。只补充已有 `/oscar/scratch/zliu328/hf_output` 路径，未下载或替换模型、修改提示或控制器。替代作业 `6525607` 已提交；失败日志保留。

替代 CPU 回放现已成功（6 分 3 秒），全部 3386 题、10158 个轮数检查点的查询与最终证据一致，零新增 LLM 调用。两份脚本与 `code_6525607.zip` 逐字节核对后从活动目录删除；保留轨迹、结果和归档。这里只完成原轨迹重建，尚未产生更换图后的结果。

### 当前重点：正确证据下的 QA

参考 [Lost in the Middle（TACL 2024）](https://aclanthology.org/2024.tacl-1.9/) 的 oracle-context 诊断，使用项目已有的 2Wiki 原生支持段落，按原数据加载器的顺序完整提供，不加入答案、不重写材料、不补标注。比较 gold-only、冻结 BM25 五条原文、当前无相似边事实图五条原文；三个条件的 QA 在同次运行重算，固定原生提示、生成设置、模型版本与答案 F1。gold-only 的段落数量由标注决定，因此不是等 token 或等段落数实验，也不能单独归因于顺序或干扰。

覆盖 HippoRAG 发布的全部 1000 题、四个 reader，而不是原始 2Wiki 的全部数据。主实验仍保留全部六任务；其他任务不凭空生成证据标签。gold-only 使用测试标注，是不可部署的诊断条件，不参与检索、主方法或 SOTA 计数。标注证据下非满分仍须结合实际答案判断，不能把原生 F1 不满分自动解释为推理错误，也不称它为严格上限。

预检 `6525861` 已提交：先核对全部问题身份、原生 gold 段落与语料成员，再在原顺序前两题上检查四个 reader、三条件的 QA；全部成功后，依赖 `6525863` 才执行 12000 条全量预测。每个作业内四 reader 串行共用一张 L40S、单 CPU，不修改已运行作业。输出为 `optimization_gold_evidence_seed42_20260919`，尚无已完成的 gold-only QA 结果。

## 4. 已测试与当前候选

首轮严格图模式检索及其语义实体、三元组附录对照已结束，结果不足以继续扩展。以下事实联合选择是假设记录，不是当前已经有效的主方法。

**研究问题：能否利用事实之间的关系，选择共同回答问题所需的证据，而不只是给原文逐条排序？**

例如，“某人的母亲何时去世”需要先找到母亲，再找到她的去世日期。同一人的其他亲属可能语义相关、在图中也很近，却不能替代这两条关系。这个例子用于说明问题，不是写入算法的模板。

候选设计分成两个可独立检验的部分：

1. **事实及出处的表示。** 保存原始主体、关系、客体以及每次出现的出处，保留方向和不同出处版本；不在建图阶段一律按加载顺序删除旧值。沿用原文作为最终证据，不把抽取三元组当作可靠事实证明。
2. **联合证据选择。** 从相同的初始检索候选出发，比较逐条排名与选择相互关联的一组事实及其出处。先用已发表的子图选择方法建立对照，再判断出处和关系约束是否提供额外价值。仅仅换成 Steiner tree 不算新贡献。

索引只读语料，不读 benchmark 问题或答案。主体/客体相等只是可连接条件，不等于语义蕴含；关系别名、否定和时间范围不能靠字符串连接自动解决。若诊断显示主要问题是答案生成而非证据选择，则缩小这个假设，而不是强行归因于图结构。

**当前最小试验：根据出处调整事实向量。** 在原有事实与原文之间按抽取出处建立二部图，用已发表的 retrofitting 更新向量；它使每个向量在保留原表示的同时接近相邻节点。采用作者实现的固定 10 次迭代和原向量/邻居平均的等权设置，按事实、原文两组依次更新，最终归一化供余弦匹配使用。不重新抽取、不读取问题建图、不训练 encoder，也不添加阈值或任务分支。

首项对照仅将得到的事实向量替换进原 HippoRAG 2 的事实索引。原文向量只在离线更新中参与计算，在线 dense passage retrieval 仍用原向量；图、recognition 提示、PPR、QA 文本及原生指标都固定。这样先回答“出处能否改善事实检索表示”，不声称已经有新的完整建图算法。风险是同一段内不相关事实被拉近，必须以全量原生 QA 检验；若有效，再比较不同图表示及其组合，不能把已有向量更新公式算成新贡献。

**图结构与事实向量的四组对照已完成，尚未选为新算法。** 分别使用原图与保留显式事实节点的图，并各自搭配原向量与已保存的上下文化向量。另列 BM25。事实节点连接主体、客体与所有原文出处，使用既有二部图构造；保留原实体到原文直连和实体相似边，替换原实体间的事实边，不做旧方法的两两投影。全部出处都保留，不加关系归一、最后出处筛选、候选删除、RRF、窗口或事实附录。

此时事实节点只参与传播，不额外分配起始权重；在同一种事实向量设置内，仅切换图结构不改变原有实体与原文的起始权重。跨两种向量设置时，候选事实、recognition 输出及实体起始权重可能变化；固定的是计算规则，不是这些数值。PPR 参数、五条证据上限和原生 QA 均不改。更换图结构本身会改变路径长度与随机游走的转移概率，不能把得分差异只解释为“保留了关系语义”。原图的两项检索复用上轮完整结果；新图复用相同 recognition 缓存，缺失则报错而不是退回其他检索。五项 QA 在同次 Gemma/L40S 运行中重算。先在每个来源组前两题检查接口并重放旧检索，再跑全部六任务 3386 题，不按预检分数调整参数。

显式事实节点的旧实验并非没做过。这一轮新增的是与出处上下文化的交叉对照，不能将重新启用旧表示写成新贡献。只有结果显示可复现收益后，才决定后续方法设计和其他 reader 的扩展。

全量结果表明，组合版对 BM25 为 5 胜 1 负，但对 HippoRAG 2 仍为 3 胜 3 负；2Wiki 有局部收益，文档 QA 和 FC-SH 回退，暂不扩展到其他 reader。随后检验的问题是：已有流程先找到相关事实，再把它们拆成实体作为传播起点，是否因此丢失了匹配事实的区分能力？对照是在同一显式事实图、同一原始索引和 recognition 输出下，分别从整条事实与其实体出发，均采用标准均匀 personalization，固定 PPR 与原文 readout。额外保留原 HippoRAG 的非均匀实体/原文混合起点对照，避免把取消其他权重规则的收益误归因于事实节点。该对照已完成 Gemma 六任务全量，不新增手写任务规则。

[NodeRAG 第 2.5 节](https://arxiv.org/html/2504.11544v1)已有入口集合上的均匀 personalization，但它使用多类入口及默认两次迭代的浅层 PPR；本轮沿用 HippoRAG 的收敛 PPR，不是其完整复现，也不能把均匀起点或事实节点本身作为新贡献。应先实测是否改善证据选择，再决定能否形成有别于已有方法的完整设计。

传播起点对照的两个新配置分别在 recognition 选中的唯一事实节点、这些事实的唯一主体/客体节点上均匀分配起始权重，其他节点为零；不使用按出处数量缩放、相似度加权或原文混合起点。两者固定同一图、原事实向量、recognition 输出、PPR damping、原文证据和原生 QA。无事实被选中时，沿用底座已有的 dense fallback，并在逐题记录中明确区分；缓存缺失仍报错，不作为空 recognition 处理。实体配置使用全部选中事实的唯一端点，不沿用旧方法的第二次实体 top-k；因此它和旧方法的差异不能只归因于权重由非均匀变成均匀。事实配置与实体配置之间不改变事实集合。

全量中，事实均匀起点对 HippoRAG 2 为 4 胜 2 负、对 BM25 为 5 胜 1 负，但对实体均匀起点只有 3 胜 1 平 2 负。MH 与 2Wiki 的主要收益并不需要事实起点才能出现，不支持将它写成统一优势；SH、LoCoMo 的回退也尚未解决。补充对照在相同均匀分配规则下，为两种起点加入相同的 dense 原文候选，区分原文信号与权重规则的影响。候选数量统一沿用已有 top-k=5，不逐任务选择，不增加可调混合系数。概率在整个唯一节点集合上均分，所以事实/实体数量仍决定其相对原文的总概率；不能称为不存在设计选择或两类总概率固定。该对照已完成并出现大幅回退，不采用此版本。

冻结的五项起点对照检索也已在 Qwen4B、Qwen9B、Llama 上分别完成 QA。每个 reader 先做六任务、15 来源组的预检，再覆盖全部 3386 题；只换 reader，不重新检索，不根据它们的结果修改已冻结输入。结合 Gemma，事实均匀起点在四个 reader 上均对 HippoRAG 2 为 4 胜 2 负，同样在 SH 与 LoCoMo 回退。该阶段只包含 BM25、HippoRAG 2、原混合起点与两种均匀起点，不等于已对全部 baseline 完成 SOTA 比较，也未验证新配置的 IRCoT。Gemma 的新增原文起点试验独立运行，不能将旧配置的其他 reader 结果当成新配置已经验证。

CPU 诊断发现，在 3068 个非空 recognition 问题中，“事实加原文”配置的最终五条材料全部来自原来的 dense 五条，图仅改变排序；其余 318 题本来就走 dense fallback。因此当前混合方式几乎没有通过图引入新证据。下一项先固定图、全部起点、PPR 和输出条数，比较总 PPR 分数与移除初始直接贡献后的分数，检验起点的直接得分是否抑制了其他材料。分解依据是已有 [PageRank 固定点关系及原文 Eq. 5](https://snap.stanford.edu/class/cs224w-readings/andersen06localgraph.pdf)；选择游走项作检索排序仍是本项目待验证的设计，不是原论文提出的 QA 方法，也不能将其图划分保证外推成 QA 保证。原论文使用 lazy random walk，本项目沿用现有 PPR，不冒充完整论文复现。

该对照预检 `6516389` 成功（3 分 14 秒）：带权图和孤立节点上的固定点等式检查通过；15 个原来源组各取前两题，原图及此前四种均匀起点检索重放通过，新旧 recognition 和全部起点逐题一致。新配置只改变最终原文排序，空 recognition 的 dense fallback 不变。六项已有检索作为参照，两个新配置为实体加原文、事实加原文的游走分数；八项 QA 在同次 Gemma/L40S 运行中重算。未根据样例调参。

全量 `6516411` 随后成功（31 分 11 秒），八项各覆盖全部 3386 题，输出目录为 `optimization_ppr_readout_seed42_20260919`。事实配置对 HippoRAG 2 仍为 2 胜 4 负，对 BM25 为 3 胜 3 负；虽修复 SH、略改善 LoCoMo，但 MH、事实整合、2Wiki 回退。3068 个非空 recognition 问题中，事实配置有 1417 个不再只返回原 dense 五条，说明去掉初始直接贡献确实改变了材料集合，但这种扩展没有转化为整体 QA 收益。该排序改动不采用，不继续调混合系数；结果与失败记录见 `results.md`。两个运行脚本已归档核对并从活动目录删除。

当前构图消融固定现有事实节点、全部主体/客体/出处连接、事实均匀起点、原索引和原 PPR，分别删除继承的实体到原文直连、相似实体边，并列两者同时删除的配置。它是两类已有边的完整四格对照，不重新抽取、增加打分公式或按任务选择图。预检 `6517772` 已成功（4 分 33 秒）：按真实边来源标记删除，15 组旧检索重放、逐题起点一致性和六项原生 QA 检查通过。15 组图共 350330 个节点，579843 条事实连接、245801 条实体到原文直连、2495217 条相似实体边；删除只改边，节点和剩余权重不变。边数占比不等于传播贡献或因果效果。

六任务全量 `6517828` 已成功（27 分 57 秒），产物目录为 `optimization_graph_edges_seed42_20260919`。六项各完成全部 3386 题：四种图配置、BM25、HippoRAG 2，固定 Gemma/L40S 重算 QA。两类边同时删除后，相对保留为 4 胜 2 平，边数减少约 82.5%；对 HippoRAG 2 仍为 4 胜 2 负，SH 和 LoCoMo 未修复，对 BM25 为 5 胜 1 负。单独删除则各有回退，不能将联合收益归因于某一类。预检未用于调参；已有事实均匀起点代码从冻结 ZIP 复用，两个已完成脚本已归档并从 scratch 活动目录删除。

冻结检索的 Qwen4B、Qwen9B、Llama 作业 `6518378`、`6518379`、`6518380` 均已完成预检和六项全量 QA，耗时 26 分 20 秒、44 分 3 秒、36 分 37 秒；原生分数核对通过，无新检索或调参。输出为 `optimization_graph_edge_readers_seed42_20260919`，完整四格见 `results.md`。同时删除两类边相对保留，在 Qwen4B/9B 均为 2 胜 1 平 3 负，不能将 Gemma 的结果外推。仅删除相似实体边在四模型的 2Wiki 均提高，但其余任务仍有回退。

统一候选为删除相似实体边、保留实体到原文直连，图边数减少约 75.1%。随后完成的九项 baseline 同协议比较表明，四个 reader 均只严格领先两项任务，未达到多数任务领先的目标；详细分数见 `results.md`。新图的完整多轮结果仍未结束。标准事实节点与 PPR 本身不作为新贡献；不能用对 HippoRAG 2 的局部优势代替完整 baseline 比较。

新图 IRCoT 接口已实现于 `optimization/retriever/fact_incidence.py`，只加载既有事实图、切换相似实体边并设置事实均匀起点，不更改多轮算法。临时运行适配器复用 `optimization.ircot` 的批量官方控制器，统一比较 BM25、未删相似边的事实图、删除相似边的事实图。每轮检索 6 条、最多累计 15 条原文，保持官方停止规则，上限 5，从同一轨迹报告 1、3、5 轮；各上限仍使用原任务 QA 提示与指标。因此这是 IRCoT 检索流程接入六任务原生 QA，不冒充所有任务的官方完整 IRCoT 问答复现，也不是工具调用 agent。没有融合或额外上下文。

复用抽取、向量和已有 recognition 缓存，新子问题正常调用本地 Qwen3-30B-A3B，服务失败直接中止，不降级伪装为空识别。BM25 和两种图的推理轨迹、各上限 QA 均重新运行。预检 Gemma `6519803`、Qwen4B `6519913`、Qwen9B `6519917`、Llama `6519918` 均成功，耗时分别为 9 分 33 秒、13 分 12 秒、10 分 27 秒、12 分 34 秒。每个 reader 的 15 组、两配置各两题共 60 次旧单次检索重放通过且零缓存缺失；六任务各第一组前两题的官方多轮控制器与 54 项原生 QA 检查通过。控制器一致性使用已生成输出重放，不等于串行与批量生成文字始终相同。重复提交的 `6519915` 已在未分配节点、零运行时间时取消，无输出。

对应全量作业已提交：Gemma `6520055`、Qwen4B `6520056`、Qwen9B `6520057`、Llama `6520060`。产物为 `optimization_ircot_fact_graph_seed42_20260919`；目前无全量完成结果，不能引用旧图 IRCoT 成绩作新图结果。新接口的测试与运行适配器均随作业归档，不新增永久 launcher。

单次 QA 同时补齐项目已列九项 baseline：BM25、dense、HippoRAG 2、Mem0、LightMem、LightMem offline、AnchorMem dense、AnchorMem official、CatRAG。复用它们各自已有完整检索缓存，保留原检索输出和条数，用同一 reader 的原生批量 QA 与新候选重算，不复用旧 QA 分数。加载模型前检查六任务全部题目与覆盖一致，缺失则报错，不缩减比较范围。Gemma `6520078`、Qwen4B `6520284`、Qwen9B `6520285`、Llama `6520286` 预检均成功，每个 reader 的 60 项原生 QA 检查通过。

单次全量作业 Gemma `6520283`、Qwen4B `6520643`、Qwen9B `6520646`、Llama `6520647` 均成功，分别耗时 24 分 53 秒、36 分 37 秒、67 分 3 秒、53 分 13 秒。CPU 汇总 `6520819` 成功，逐题核对全部 135440 条预测的原生评分、题目、证据和用量；输出为 `optimization_fact_graph_main_qa_seed42_20260919`。四模型均对 BM25 为 5 胜 1 负，但对九项最佳仅 2 胜；Qwen9B、Llama 各另平一项。该候选未达标，暂不将其定为新主算法。

因 `gpu-he` 用户配额满，后三个 reader 的未启动预检调到账户允许的 `gpu` / `norm-gpu`，成功后全量先沿用该分区。Gemma 完成后，将仍排队的 Llama 全量改回 `gpu-he`，未重启任何运行中作业。所有 reader 均为同种 L40S、单 CPU，分区改变不改变模型、数据、推理设置或检索输出。IRCoT 汇总 `6520821` 等待四项多轮作业全部成功，不用部分任务宣布完成。

只读诊断 `6522293` 已成功，LoCoMo 官方题型表明损失同时存在于单跳、多跳和时间题，不能只补日期规则。接下来的预检 `6522450` 固定已有检索结果，交叉比较旧/新检索流程与原文/旧版已整理上下文；不新增规则或抽取，先隔离既有上下文模块的作用。旧检索包含候选筛选与 RRF，因此这不是纯图结构的两因素实验。原生 QA 和全部六任务不变；预检通过后再跑全量，不以预检分数调参。新图 IRCoT 仍按现有配置继续，不重启或拼接不同流程。

预检 `6522450` 的 15 组来源、30 题和四配置原生 QA 检查已通过；分数有升有降，未据此调整方法。全量 `6522517` 已提交，四格各跑完整六任务 3386 题。只有完整对照结束后才判断是否值得继续测试上下文模块在多轮中的作用，不把恢复旧模块当作新贡献。

该全量现已完成，新检索恢复旧上下文后为 3 胜 1 平 2 负：FC-SH 48→56、LoCoMo 38.33→39.57，但 FC-MH 4→3、2Wiki 50.40→43.50。QA 输入从约 367 万增加至 1125 万 token，因此不采用整套恢复。固定旧上下文，新检索仍对旧检索为 1 胜 1 平 4 负，问题不能全部归因于上下文模块；新旧检索还存在候选和融合等差异，不能将差距只归给图结构。

下一项检索对照先核对 [G-Retriever（NeurIPS 2024）](https://proceedings.neurips.cc/paper_files/paper/2024/hash/efaf1c9726648c8ba363a5c927440529-Abstract.html) 的 PCST。官方实现固定为提交 `315b0ff8a206536067602fb97e77c10f4d646d5d` 的 `src/dataset/utils/retrieval.py`：节点排名奖励、默认 top-k=3 和边代价 0.5 仍是经验设置，标准优化目标不等于不存在设计选择。当前事实连接边没有独立的语义向量，不人为编造边向量；先检查原代码支持的 `topk_e=0` 节点奖励版本。准备作业 `6523352` 只安装独立依赖、保存官方代码并做小图接口检查，尚未运行六任务检索或 QA，也不代表完整 G-Retriever 复现。正式实验前须明确子图如何返回原文、与 PPR 的候选及证据单位有哪些差异，不将多项改动误报为纯搜索算法消融。

首次预检发现求解器的 NumPy 数组接口错误；保持 `pcst-fast==1.0.10`，以 `pybind11==2.13.6` 在新目录重新编译，上游复现例及连接小图的索引检查通过。没有修改奖励、边代价或现有环境；失败记录见 `results.md`。

接口修复后按原协议重新预检，覆盖六任务、15 组各前两题。当前适配明确限定为原文节点奖励：沿用已保存 dense 检索的前三条原文，按官方排名方案赋予 3、2、1，其他节点为零；在同一事实图拓扑上，PCST 使用单位连接代价 0.5，PPR 使用无权边和 damping=0.5。两种新对照不使用 recognition，原有加权、事实起点 PPR 另外保留为完整参考。输出都为原文，PCST 按原节点顺序输出其选中的原文，要求 1–5 条，否则报告不兼容，不增设截断或补齐规则；PPR 返回前五条，因此只是共同上限，不是逐题等证据数量。BM25、原 dense 也作为原生 QA 对照。该试验检验现有图作为连接结构能否改善原文选择，不是原版 G-Retriever 的全部节点/边奖励、GNN 或软提示复现，也不是只替换现有完整方法的一个算子。完整原题覆盖在选择预检题之前核对，不改变数据、问题或指标。

检索预检 `6523947` 已通过，30 题均返回 2–5 条原文；Gemma 预检 `6523988` 完成五配置共 150 条预测，原生输入与评分检查通过。另发现其中 25 题的 PCST 原文集合等于 dense 前三条，因此增加直接使用 dense 前三条的消融，不能将相对 dense 前五条的变化都归于图。该对照沿用原 dense 排序，PCST 沿用官方节点顺序，仍须注意证据顺序差异。新增对照的检索预检为 `6524109`，QA 为 `6524110`；其余五配置复用相同输入下已完成的预检答案，不按这些小样本分数调参。

上述固定上限版本的全量检索 `6524127` 在 MH 第 38 题停止：选中子图含 6 条原文，超过预设的五条上限；依赖 QA `6524128` 未执行并已取消。失败记录与全部旧缓存保留。重新核对官方代码后，`topk=3` 是获得奖励的节点数，不是最终子图大小；这个接口限制不属于 PCST。本次失败只能说明固定五条的接口不兼容，不能说明 PCST 的 QA 效果差。

当前协议另存 `native_subgraph`：保留完整选中子图所对应的全部原文，不截断、不补齐，节点奖励、边代价和排序不变。PCST 每题 `top_k` 记录实际原文数；不同 k 的段落指标不合并成检索质量结论，只比较原生答案指标，同时报告原文数与 QA token 用量。这不再是等原文数或等 token 预算实验。六配置的 30 题检索预检 `6524329`、Gemma QA `6524330` 均通过，全量检索 `6524409` 已启动，Gemma QA `6524417` 等待其成功。六任务、3386 题和原生评分均不变，没有依据预检分数调参。

全量检索 `6524409` 成功（5 分 48 秒），3386 题均完成，原文数为 1–6；Gemma 全量 QA `6524417` 已成功（15 分 15 秒），六配置共 20316 条预测通过原生检查。事实节点只出现在 45 个选中子图中。CPU 消融 `6524655` 删除全部事实节点，固定其他设置，结果为 2857 题检索记录完全相同、529 题原文集合变化。该检查覆盖全部六任务，但没有新增 QA，不能声称删除无损；因主方法明显回退，目前不继续扩大这项分支。结果见 `results.md`，缓存为 `native_subgraph/without_fact_nodes`。

完整六任务显示原文奖励 PCST 明显回退：对 BM25 为 1 胜 1 平 4 负，对上一版事实图 PPR 和相同原文起点 PPR 均为 1 胜 5 负，对已有九项 baseline 的逐任务最佳为 0/6。该分支不采用、不扩展到其他 reader 或 IRCoT，不把换优化目标包装成成功。下一步的信息表示检查可参考 [Dense X Retrieval 的已发布模型](https://github.com/chentong0/factoid-wiki)，但目前只核对论文、模型与环境，没有新增信息抽取或全库重建，也未修改原有四模型 IRCoT 作业。

原五配置预检另存 `initial_pilot_before_dense_top3.zip`；新检索器完整逐题耗时未测量，记录为缺失，不继承 dense 的时间。构建文件 47 行、四模型 baseline 评测脚本 109 行、CPU 消融脚本 107 行、PCST 评测脚本 206 行均在归档逐字节核对后删除，数据与结果保留。

## 5. 相关工作约束

### 重新检查的信息表示预检

吸收排序全量未达标后，先恢复这里尚未真正生成过文本的兼容性预检，不扩大抽取范围。已确认原失败发生在 Transformers 新版生成配置的断言，且日志包含权重绑定行为差异；不是生成质量失败。现有 `ircot` 环境使用 Transformers 4.51.0，采用此环境重新执行作者示例的贪心生成，不修改权重、生成参数或原文。作业 `6534251` 在 `gpu-debug` 用一张 L40S、单 CPU、32 GB、55 分钟上限，结果另存 `transformers4`，旧失败保留。只有 30 条原文的格式与成本检查，无 QA 或全库抽取；预训练抽取器的使用也不能称为我们训练了新模型。下方暂停说明保留历史。

`6534251` 因该环境缺少 SentencePiece 在 tokenizer 初始化时失败，未生成文本。替代作业 `6534297` 只引用现有 Python 3.11 环境中的 SentencePiece 0.2.2 及其包信息，已检查 Transformers 仍为 4.51.0；没有安装、升级或覆盖环境。模型、输入及生成设置不变，原失败日志与代码归档保留。

`6534297` 已完成实际生成，但最后完整性检查失败：22/30 条完整，四项 MAB 的 8 条均达到示例的 512 输出 token 上限；LoCoMo 样例另有说话人与对话对象混淆及无原文支持的内容。格式问题不能代表模型质量，而这些语义错误也不能只靠放宽生成上限解决。先不做全库抽取，保留原始输出，不补齐 JSON 或丢弃失败语料。耗时与具体例子见 `results.md`。

下一项低成本检查先复用已有 OpenIE 和向量：依据 Dense X Retrieval 第 4.3 节的出处聚合，按原文所含事实的最大查询相似度对原文排序，再返回前五条不同原文，检查不经过图传播的事实索引。这是将已发表的聚合方式用于已有三元组的组件对照，不是作者的完整 proposition 管线或新算法；固定六任务题目与原生 QA，不新增抽取、分词或数据划分。先核对原文与事实映射，再做接口预检和完整 QA。若这种直接出处检索已经解释收益，则不能将它归于图传播。新排序还未实现或得到结果。

[Dense X Retrieval（EMNLP 2024）](https://aclanthology.org/2024.emnlp-main.845/) 已研究自包含独立陈述作为检索单位；本轮先检查其 [作者发布的 Propositionizer](https://github.com/chentong0/factoid-wiki)，不把陈述抽取本身当作新贡献。固定模型 `chentong00/propositionizer-wiki-flan-t5-large` 的发布修订 `aaa3ddeef7ac710cdbfa3ae306fa2c6fec21884a`。该模型尚未在现有 HF 缓存中，需下载约 3.13 GB 权重；已有环境具备 T5 与 sentencepiece，不改环境依赖。

准备 `6525095` 已完成（3 分 17 秒），核对原有六任务的完整问题数量与 14362 条源文档，再从 15 个语料组按原顺序各取前两条原文，共 30 条，仅作格式与成本检查。GPU 预检 `6525097` 使用一张 L40S、单 CPU，本地 Hugging Face 推理，不调用外部推理 API；20 秒后在生成配置属性断言处失败，尚未生成输出，不是抽取质量或 QA 失败。失败日志与权重保留，没有按失败结果修改模型设置或再次提交。

原预检方案只向模型提供原文，不接收问题或答案；不重新切分、不截断、不更改数据划分。拟沿用作者的输入格式，标题和小节留空、完整原文放入 Content，并使用 float32、贪心生成与 `max_new_tokens=512`，批量大小为 16。由于检查未通过，不能声称这些生成设置已成功执行。依照用户关于 LLM 依赖的反馈，此分支暂停，不扩为全库抽取；优先进行上面的在线筛选消融。

保存每条原始输出、解析状态、结束标记和 token 用量；解析失败或没有正常结束就保留失败记录，不修补、不重试成更容易通过的输出、不用空列表冒充成功。结构检查不能证明语义忠实性，后续仍需对照原文检查。此轮没有全库抽取、新图或新 QA 结果，原四模型 IRCoT 继续运行。输出目录为 `optimization_propositions_seed42_20260919`。

暂停后，两份临时脚本与 `code_6525097.zip` 逐字节核对一致并从活动目录删除；保留模型权重、原输入、准备结果和失败日志，不将未执行的生成步骤当作结果。

| 工作 | 已经做了什么 | 本轮需要避免的重复主张 |
|---|---|---|
| [KG2RAG](https://arxiv.org/abs/2502.06864) | 三元组与 chunk 对应，图扩展，最大生成树组织上下文 | 保存出处、跨 chunk 连接、组织相关材料本身不是新贡献 |
| [G-Retriever](https://arxiv.org/abs/2402.07630) | 用 Prize-Collecting Steiner Tree 选取相关子图，结合图编码与 LLM | PCST 不是新算法；只复用它的检索算子也不等于复现完整 G-Retriever |
| [SubgraphRAG](https://proceedings.iclr.cc/paper_files/paper/2025/file/11e1900e680f5fe1893a8e27362dbe2c-Paper-Conference.pdf) | 面向 KGQA 的子图检索 | 子图比单条三元组更适合回答问题不是新的问题定义；具体方法仍需逐项核对 |
| [Provenance Semirings](https://web.cs.ucdavis.edu/~green/papers/pods07.pdf) | 区分关系查询中的替代来源与联合依赖 | 可用于准确表达依赖，不能直接推出自然语言答案正确性；只记录出处不足以声称用了该理论 |
| [HyperGraphRAG（NeurIPS 2025）](https://proceedings.neurips.cc/paper_files/paper/2025/file/df55ee6e59f8ac4a625219e11fe9ddba-Paper-Conference.pdf) | 抽取自然语言描述的多元事实，以二部图保存；检索实体和事实并结合原文 | 显式事实节点、二部图和无损保存成员关系本身都不是新贡献；仅把现有三元组改为节点不等于恢复被遗漏的条件 |
| [HGRAG](https://arxiv.org/abs/2508.11247) | 实体作节点、段落作超边，结合实体及段落语义进行扩散 | 不能把跨粒度图扩散或段落与实体联合建模称为首次提出 |
| [NodeRAG](https://arxiv.org/abs/2504.11544) | 将语义单元、实体、关系、原文及摘要等表示为不同节点，结合多入口检索和浅层 PPR | 显式关系/事实节点与异构图检索已有先例；本轮不复现其抽取、摘要、语义边和检索流程 |
| [Retrofitting](https://aclanthology.org/N15-1184/) | 根据语义图约束调整已有向量 | 当前使用已有更新方法；事实与出处是本试验的输入适配，不是新优化公式 |

[作者实现](https://github.com/mfaruqui/retrofitting/blob/master/retrofit.py)的更新可在二部图两侧分别批量执行，但必须保持先事实、后原文的顺序；不是把同步迭代误称为相同算法。[Embedding Retrofitting for RAG](https://arxiv.org/abs/2601.15298)也已讨论此方法在 RAG 中对图质量的依赖，因此“图调整 embedding 用于 RAG”不能作为本项目的 novelty。当前不采用其数据清洗、阈值或新评测数据，仍使用项目原始六任务。

进一步核对全文：[SAG](https://arxiv.org/abs/2608.12129) 用事件和实体索引执行查询时连接，再由 LLM 联合选择候选；[PAGE-RAG](https://arxiv.org/abs/2608.29753) 在候选材料上建图，结合相关性、出处、常见连接实体等信号筛选证据。两者都与本轮方向直接相关，不能将“连接事实并筛选证据”当作新贡献。尚未完成这两项的代码复现。

[SimGRAG](https://aclanthology.org/2025.findings-acl.163/) 已将自然语言问题转换为图模式并检索匹配子图，[RoG](https://arxiv.org/abs/2310.01061) 已用关系路径规划检索。因此，后续若增加查询解析或关系连接，也不能仅凭这两个操作声称 novelty。需要验证具体表示、原文出处和联合选择之间的作用，而不是换成 DB 或 compiler 术语。

进一步核对 SimGRAG 的论文与官方实现后，不能把当前候选当作其完整复现：

| 项目 | SimGRAG | 当前候选 |
|---|---|---|
| 已知实体 | 语义向量近邻，不要求同名 | 沿用文本归一化后要求名称相同 |
| 匹配评分 | 已知节点距离与已知关系距离之和 | 整条三元组相似度，或仅关系距离 |
| 图结构 | 论文按无向结构匹配；代码使用简单图 | 有向事实连接，保留每个关系条件 |
| 节点复用 | 论文定义一一对应；代码允许并单独选择部分节点复用结果 | 不要求不同变量指向不同实体 |
| 多个不连通部分 | 官方代码分别检索后合并结果列表 | 当前搜索要求全部条件获得匹配 |
| 最终证据 | 匹配子图的三元组 | 对应的原文出处 |

依据为[论文第 4 至 5 节](https://arxiv.org/html/2412.15272v2)、[官方检索代码](https://github.com/YZ-Cai/SimGRAG/blob/1dd95cc63240b19393f2527a775805c850a633a2/src/retriever.py)及[结果选择代码](https://github.com/YZ-Cai/SimGRAG/blob/1dd95cc63240b19393f2527a775805c850a633a2/src/utils.py)。官方简单图会覆盖同一对节点的多个关系，全部未知节点且无类型候选时也会报错；接入时不能静默丢条件或丢问题。其论文使用人工整理的 KG，而当前语料来自 OpenIE，适用性需要实测。这些是实现差别与风险，不自动构成新贡献。当前优先确认精确名称匹配和查询结构是否造成覆盖损失，而不是继续围绕小样本添加规则。

[Don't Ask the LLM to Track Freshness](https://arxiv.org/html/2606.01435v1) 已研究抽取候选事实后按显式版本号选最新值，并在多跳分解的每一步执行该操作。因此，确定性更新处理和逐跳版本选择本身也不是新贡献。该文的部分对照同时改变提示、输出格式和温度，不能直接据此认定更新算子是唯一收益来源；其当前值选择也不适用于所有历史、计数和比较问题。本地 FactConsolidation 沿用的原生提示确实要求较大事实编号优先，但不能把该任务约定无条件推广到其他五类语料或把原文加载顺序等同于事件时间。本轮不修改 loader、切分、任务提示或评分。

## 6. 实验顺序与停止条件

1. 完成上述失败诊断，并检查 OpenIE 缓存是否保留新表示需要的信息。只在明确缺失时考虑局部重新抽取，不先重跑全库。
2. 先在固定候选材料上检查排序与联合选择的作用，避免将召回变化和上下文变化混为一谈；再实现一个最小候选和对应的已发表算子对照，复用现有检索接口。先做每个任务的接口预检，确认输出是原语料、没有读取答案，再运行六任务全量。
3. 固定初始候选、reader、原文格式与检索数量，只改变表示或选择方法。新增 reranker 或额外模型调用必须同时给 baseline 对应对照，并报告开销。
4. 同时看单次 QA 和 IRCoT，先覆盖 Qwen4B/Gemma 中已暴露的问题，再覆盖另外两个 reader；前期两模型试验不冒充四模型最终结果。
5. 只有两条流程都有值得保留的证据后，才扩展模块与参数，再做单项和必要的联合消融。无有效收益的模块删除，实验结论与失败记录保留。
6. 若只靠更大的上下文、reranker 或 BM25 融合提升，而换掉新图仍一样好，就不将收益归因于新图。若多数任务仍输 BM25，则放弃候选，不靠挑轮数宣布成功。

## 7. 当前执行记录

- 冻结起点对照的 reader 扩展已全部成功：Qwen4B `6514597`（24 分 5 秒）、Qwen9B `6514598`（38 分 24 秒）、Llama `6514599`（32 分 29 秒），均通过预检并完成五项各 3386 题的原生 QA。事实均匀起点对 HippoRAG 2 均为 4 胜 2 负，对 BM25 分别为 4 胜 1 平 1 负、5 胜 1 平、5 胜 1 负。来源是冻结的 `optimization_fact_seeds_seed42_20260919/main`，输出在 `optimization_seed_reader_transfer_seed42_20260919`。没有新检索或抽取调用，不是全 baseline SOTA 结论。
- 原文起点预检 `6514606`（3 分钟）和全量 `6514609`（26 分 30 秒）已成功，六项各完成 3386 题原生 QA。直接加入原文的事实配置对 HippoRAG 2 仅 2 胜 4 负，SH 50、MH 32、FC-SH 25、FC-MH 4、LoCoMo 40.31、2Wiki 40.25，不采用此版本。CPU 诊断 `6516229`（11 秒）确认其非空 recognition 的 3068 题最终出处集合全部等于 dense 起点集合；完整记录在 `optimization_passage_seeds_seed42_20260919/passage_seed_audit.json`。六个已结束的临时入口均检查归档后删除，生产代码未改。
- 传播起点全量 `6514394` 已成功（23 分 41 秒），五项各覆盖六任务全部 3386 题，原生评分重算通过、两新配置 recognition 事实逐题一致，无缓存缺失或新 recognition 调用。事实均匀起点对 HippoRAG 2 为 4 胜 2 负、对 BM25 为 5 胜 1 负，但 SH/LoCoMo 仍回退；与实体均匀起点比较为 3 胜 1 平 2 负。全量分数、token 成本及边界见 `results.md`。临时脚本已核对归档并删除，生产代码未改。
- 传播起点预检 `6514393` 已成功（3 分 11 秒）。Gemma/L40S、单 CPU，五项 QA 均完成原生核对；15 组旧检索重放通过，两个新配置逐题 recognition 事实相同。30 题中 3 题为空 recognition，按原流程用 dense；其余 27 题的实体与事实起点互不相交，21 题的最终出处排序不同。样例实体/事实起点在 LoCoMo 均为 24.54、2Wiki 均为 0，未显示明确收益，未据此调权重。输出为 `optimization_fact_seeds_seed42_20260919`。
- 图结构与事实向量的全量 `6514252` 已成功（25 分 28 秒）：五项各完成六任务全部 3386 题，原生评分重算通过，无 recognition 缓存缺失或新调用。显式事实节点配原向量对 HippoRAG 2 为 3 胜 1 平 2 负；配上下文化向量为 3 胜 3 负，后者对 BM25 为 5 胜 1 负。组合版 2Wiki F1 为 46.75，对照 HippoRAG 2 为 44.37，其他三项仍落后；暂不扩展该版本。完整表及 token 用量见 `results.md`，图、预测和运行归档保留，已完成的临时入口删除，生产方法代码未改。
- 图结构与事实向量四组对照的预检 `6514243` 已成功（3 分 29 秒）：Gemma/L40S、单 CPU，附加 BM25，共五项；43 项测试通过、3 项旧产物依赖测试跳过。15 组、30 题的原图两项检索重放通过，recognition 缓存缺失为零，五项原生 QA 均完成。显式事实节点的原向量/上下文化向量在 LoCoMo 为 29.13/28.97，在 2Wiki 为 75/25，其他四任务分别均为 50、50、50、0；样例不支持明确收益，未据此调参数。
- 新索引预检 `6513673` 已成功（3 分 23 秒）：Gemma、一张 L40S、单 CPU，六任务 15 组各前两题，共 30 题。原 HippoRAG 2 / 上下文化索引分别为 SH 50/50、MH 50/50、FC-SH 50/50、FC-MH 0/0、LoCoMo 31.29/28.97、2Wiki 75/25。样例结果不积极，不凭它调参数或宣称效果；2Wiki 两组均找齐了这两题的标注段落，QA 仍有差异。
- 全量 `6513732` 已成功（38 分 18 秒），三项各覆盖六任务全部 3386 题。上下文化索引对 BM25 为 5 胜 1 负，对 HippoRAG 2 为 3 胜 3 负：SH 84→81、MH 50→45、FC-SH 45→44、FC-MH 3→4、LoCoMo 40.15→40.85、2Wiki 44.25→45.37。2Wiki passage Recall@5 从 0.74975 到 0.76375。这个方向有局部收益，但未达多数任务超过 HippoRAG 2 的要求，不采纳为主方法、不宣称 SOTA；不能按任务选择原索引或新索引。
- 15 组离线索引覆盖 190345 个归一化事实、14362 段原文、199330 条唯一事实/出处连接；更新计算合计 16.35 秒，不含加载与原始抽取成本。全量直接复用这批索引。两个图后端合计新增 recognition 请求 3302 次，全部成功且有用量记录；这是合并计数，不能全部算在新方法上。完整代码、实际向量、预测与成本保留在 `optimization_contextual_index_seed42_20260919`，ZIP 已校验，临时 Python / sbatch 已删除，主算法未改。
- 旧四模块消融汇总 `6509165` 和未融合 IRCoT 汇总 `6510847` 均已成功，四个 reader、六任务完整。模块结果见 `results.md`；`without_graph` 是换回原图，不是完全不用图，`without_evidence_context` 同时移除融合、窗口和附录。不能把整组差值归因于其中一个操作，也不能将旧消融计为新候选的证据。
- 旧图加 BM25 的 IRCoT 四模型汇总 `6508961` 已成功，全部六任务及 1/3/5/7 轮完整，报告标记逐题评分已重算。统一 3 轮对同次 IRCoT + BM25：4B 为 6 胜，9B 为 4 胜 2 负，Gemma 为 4 胜 1 平 1 负，Llama 为 5 胜 1 负。完整轮数与 3 轮分数见 `results.md`。它已满足“多数情况下优于 BM25”，不需要逐任务挑轮数；仍是旧方法的结果，不能冒充新算法或全领域 SOTA，也没有隔离图结构和融合各自的贡献。
- 新事实候选的读出对照 `6513407` 已成功（5 分 4 秒），Gemma、一张 L40S、单 CPU。固定关系检索的全部 3386 题，原文加三元组相对原文为 1 胜、2 平、3 负：SH 30→30，MH 30→28，FC-SH 53→53，FC-MH 14→13，LoCoMo 23.75→24.13，2Wiki 43.91→43.12。附录增加 74493 个全量输入 token，没有改善整体表现，不采纳。两组 2Wiki 段落 Recall@5 均为 0.588，说明读出对照没有混入检索来源变化；不能把单次小幅涨跌声称为显著结果。
- 此次先通过 46 项测试（另 3 项旧产物依赖测试跳过）及六任务 30 题接口预检，再完成全量。无后备检索，原生题目、评分、排名与五条出处上限不变；实际 reader 输入和原始出处分开保存。执行代码、测试及 launcher 已归档并通过 ZIP 完整性检查，临时脚本删除；本轮新增的读出接口和专用测试也从活动代码移除，恢复到实验前已验证版本。预测、缓存、token 成本及原始请求保留在 `optimization_fact_context_seed42_20260919`。
- 取舍：停止扩展当前严格图模式候选到其他 reader 或 IRCoT，不再靠调关系分数、加实体候选或附录挽救该版本。保留失败证据。下一步回到构图侧，先核对已发表的段落与事实图方法及其评测协议，再决定原图/新图对照；查询生成的关系模式不再作为必须满足的硬入口。尚未选定新的替代算法，不将旧融合结果改名为新方案。
- 缓存回放 `6513113` 已完成，核对 10158 条原始预测。统一退回 BM25 或 HippoRAG 2 后，各自相对该后备方法均为 4 胜、2 负，但对历史九项 baseline 的逐任务最佳，HippoRAG 2 后备仍只超过三项。完整估算表见 `results.md`，不冒充重新运行的 QA，不把历史运行条件差异藏起来。
- 读出对照的动机：原程序保存了选中的三元组，却只把出处原文交给 reader；检查两种输入的区别，不再改查询解析或相似度。三元组仍是模型抽取结果，不当作验证过的真值；每条附加三元组必须在对应出处的原 OpenIE 记录中出现。
- GPU 全量 `6513034` 成功（13 分 22 秒），四项各覆盖全部 3386 题，详表见 `results.md`。关系评分对 BM25 3 胜、3 负，对 HippoRAG 2 2 胜、4 负：FC-SH/FC-MH 为 53/14，但 SH/MH/LoCoMo 明显回退。无后备检索的固定图模式版本不作为主方法；不能只引用事实整合的收益。
- CPU 回放 `6513113` 已提交，重算并核对已有逐题原生分数，只按关系检索是否为空切换到统一的 BM25 或 HippoRAG 2，检查覆盖不足的影响。分支不读取答案、不按任务选择；缓存回放不是新的端到端 QA。先看回放是否值得投入真实 QA，不新增评分权重或问题模板。原 HippoRAG 实现自身也有事实候选为空时回退普通段落检索的路径，但此处不把后备机制称为新贡献。
- 语义实体 QA 预检 `6512931` 已完成（4 分 15 秒），完整四项六任务样例表见 `results.md`。匹配从 23/30 增至 26/30，但 QA 无任务上涨，SH、MH、2Wiki 回退；当前不扩展这个版本，不据接口通过或覆盖提高称为成功。预检编码得到的关系向量仍可复用，全部失败证据保留，launcher 已归档删除。
- 原严格匹配全量 CPU `6512715` 在编码阶段正常响应中断，保留完整 SH 和所有已提交缓存；实际 Slurm 为 `FAILED / 0:2`，运行 30 分 7 秒，已记录新增编码 18491 条及编码时间 1714.49 秒。未启动的 QA `6512717` 取消，旧 IRCoT 作业不动。恢复作业 `6513034` 使用一张 L40S 先检索后 QA，保持 BM25、HippoRAG 2、整条三元组、关系评分四项和全部 3386 题，输出另存为 `optimization_fact_search_gpu_seed42_20260919`；读取 CPU 已提交缓存及语义预检生成的 GPU 缓存，不重算全库关系，不把中断前成本清零。默认算法未改，设备差异记录为执行条件，不能声称跨设备数值严格相同。
- 语义实体预检 `6512931` 已提交：固定六任务 15 组各前两题，共 30 题，比较原 BM25、HippoRAG 2、严格实体约束 + 关系距离、语义实体候选 + 节点/关系距离。同一解析、关系编码、beam width=8、五条出处上限及 Gemma/L40S QA；不添加 fallback、RRF、重排或新的提示。实体候选上限固定为 SimGRAG 官方 FactKG 配置的 16384，不在样例上调优；仍非完整 SimGRAG。新关系/查询向量用同一 Qwen3-Embedding-0.6B 在 L40S 编码，复用已有实体向量与已保存 CPU 缓存，记录实际编码设备和成本，不声称跨设备数值完全一致。
- 实验复用 scratch 的 `evaluate_fact_search.py` 入口，预检输出独立位于 `optimization_semantic_fact_search_seed42_20260919/pilot`。GPU 编码与 QA 顺序执行，编码进程退出后才加载 reader，不增加单作业内并发。原严格匹配全量 `6512715` 保持 CPU 协议，已完成 SH：两种评分均为 51 题匹配、48 无连接、1 解析无效；这不是 QA 分数。其依赖 QA 的默认参数未改。
- 全量解析 `6512502` 已成功（41 分 21 秒），结构检查 `6512775` 成功（37 秒）。3386 题中 245 题为空/非法模式，另有 750 个合法模式的已知名称不在要求的主体/客体位置出现。至少 995 题无法完成当前严格匹配，但不能称为 QA 错误率或原文缺答案；逐任务表已写入 `results.md`。这支持优先检验语义实体候选，而不是继续只调整关系评分。解析及检查脚本已归档删除。
- `FactJoinSearch.search` 新增可选的已知实体距离表，沿用 SimGRAG 的已知节点距离项，每个实体只计一次，未知变量不计，跨关系共享绑定；默认严格匹配行为不变。仍是有向 beam search，不是完整 SimGRAG 或新理论。检索 `6512715` 在改动前已启动并加载旧实现，协议与缓存均不变。
- CPU 预检 `6512895` 成功（31 秒）：43 项测试通过、3 项旧产物依赖测试跳过，覆盖共享实体绑定、节点距离不重复计分、自环及默认路径。全部 15 组共 145623 个归一化实体均已有对应向量，无缺失，维度与归一化检查通过，可复用原实体索引，不需全库重编码。产物为 `optimization_semantic_fact_search_seed42_20260919/cache_check.json`，检查脚本已归档删除。尚未运行语义候选的真实检索/QA，不据此宣称涨分。
- 已提交全量单次检索 `6512715`（batch 单 CPU，依赖解析 `6512502`）和 Gemma QA `6512717`（一张 L40S，依赖检索成功）。四项为原 BM25、HippoRAG 2、既有整条三元组评分、既有关系评分；六任务共 3386 题，原生五条出处上限、提示与评分。先检查全部任务的题目、顺序和来源组一致性，再检索；不更改提示、beam width=8 或评分公式，不加融合、fallback、重排器。无匹配、解析无效及超出出处预算的问题保留空证据并分别计数。这是候选的全量检验，不是已经确定的新主算法；当前两项 baseline 也不替代最终九项完整 baseline 比较。
- 检索复用原始事实向量及之前 CPU 编码的查询/关系缓存；缓存读入内存，新增向量分批提交，完整来源组可恢复，未完成组重算。查询解析仍属于在线成本。运行脚本仅位于 scratch，完成后归档删除；语法检查已通过，单元测试会在 CPU 作业内先执行。全部输出另存为 `optimization_fact_search_full_seed42_20260919`，不覆盖过去的主实验或 30 题预检。
- 全量解析已完成 LoCoMo 全部 1986 题：1759 合法、114 空模式、113 非法，2Wiki 继续。空模式与非法模式共 227 题，不能删去，也不能从 1759 个合法格式推断有多少能检索到正确事实；本轮不根据这些问题新增特例。
- 全量固定证据对照 `6512216` 已成功完成六任务八项 QA（恢复运行 37 分 8 秒）。同用重排前六条，旧图相对 BM25 为 5 胜、1 负；相对未处理 BM25 虽也是 5 胜、1 负，落后任务不同，不能混用。完整四处理和两后端表已更新到 `results.md`。这是旧轨迹诊断，不是新算法成绩，不据此宣称四 reader 多数领先。
- 融合预检 `6512301` 已成功完成（2 分 27 秒）。30 题上两项图融合均改善 LoCoMo，但 MH 与 2Wiki 回退；不按任务选融合或图单独使用。保持完整五项对照，暂不将融合认定为主方法。相同关系候选在两次 QA 的 LoCoMo 分数仍有变化，已记录；不将固定 seed 解释为确定性保证。
- 四项 MAB 共 400 题已解析完：SH 为 99 合法/1 非法，MH 为 87/13，FC-SH 为 100/0，FC-MH 为 98 合法/1 空/1 非法；这不是检索成功率。下一步用冻结组件的全量对照判断去留，避免继续围绕同一 30 题调规则。

以下为历史过程记录，运行状态以本节上述最新记录及 `results.md` 为准。

- 全量查询解析准备 `6512502` 已启动，范围为六任务全部 3386 题，复用现有 30 题解析缓存及同一固定提示。每次只发送问题文本，不读取答案来解析；非法模式保留，不重试到成功，不删问题。已完成 SH 的 100 题（99 合法、1 非法），其余任务继续。产物为 `optimization_query_patterns_seed42_20260919`。这批解析属于查询时模型调用，批量缓存不意味着零在线成本，也不是使用问题参与离线语料构图；后续四个 reader 的单次 QA 可复用相同查询解析。
- 固定原文全量对照已完成四个 MemoryAgentBench 任务及 LoCoMo 的全部八项 QA。只重排全部证据时，旧图在四个 MAB 任务均小幅上升，但 SH 仍低于同处理的 BM25；LoCoMo 从 F1 40.00 到 40.85，BM25 从 38.79 到 37.96。小样本上的图重排增幅未在全量上维持同等幅度。2Wiki 尚未全项结束，不报告六任务胜出数；完整阶段表见 `results.md`，不据此调每个任务的处理方式。
- 关系评分 QA `6512188` 已成功，完整样例表见 `results.md`：LoCoMo 从 17.12 到 21.46，仍低于 BM25 的 23.52；FC-SH 上升而 SH 回退，其余样例不变，不能视为全面改进。后续 `6512301` 固定候选比较图单独使用与标准 RRF 融合，沿用现有常数 60 和六条出处上限，不再调评分或解析。BM25 与两项图单独对照均同次重新 QA。此假设只检验图作为补充检索的价值；融合算子本身不是新贡献，也不能据融合上涨证明图表示优于逐条事实检索。
- 缓存恢复 `6512216` 已实际载入 28226 条已提交分数，原四项 MemoryAgentBench 两后端及 LoCoMo/BM25 无需重新评分，已存在的检索文件完全一致。新生成的分数仍逐批提交，不把中断前的模型成本清零。
- 恢复作业随后完成全部六任务证据准备并进入全量 QA，本次记录 35305 对模型评分（含四对批量检查）、7578034 输入 token、纯 forward 168.12 秒；与中断前用量分开保留。缓存读取优化没有改变评分模型、处理顺序或已存检索输出。CPU 测试 `6512339` 成功：41 项通过、3 项旧产物依赖测试跳过，覆盖无原始分数排名、零分过滤及重复条目检查；临时测试 launcher 已归档删除。
- `6512008` 已完成全部 30 题，旧三元组评分的前五个候选全部精确重放一致。新关系评分同为 23 题有匹配、6 题无匹配、1 题解析无效；这些计数不是 QA 效果。共新编码 2397 个文本，模型编码计时 151.78 秒，不包含数据库读取、加载和其他处理。运行代码与脚本归档在该输出目录，已完成临时脚本从活动目录删除。依赖 QA `6512188` 保持原协议。
- 全量原文对照 `6511907` 在重复读取共享盘 SQLite 缓存处收到正常中断；`finally` 已保存模型用量：28230 对、7353289 输入 token、纯 forward 100.18 秒，Slurm 总运行 15 分 23 秒。该次记录为中断而非成功，不丢弃成本。只将重复分数查询改为启动时一次读入内存，新分数仍每批落盘；不改变任何候选、模型或评分。恢复作业为 `6512216`，复用同一已提交数据库，并逐字核对已存在的检索文件。旧 IRCoT 和其他消融作业未取消。
- 关系评分对照 `6512008` 已提交并运行：固定上轮 30 题解析、主体/客体约束、beam width=8 和出处上限=6，先重放整条三元组评分并核对前五个候选，再替换为 SimGRAG 已发表的关系向量 L2 距离项。使用相同 Qwen3-Embedding-0.6B，不重新生成模式，不增加别名规则或阈值；关系向量按需计算并缓存。该试验不是完整 SimGRAG 复现：没有其节点语义候选、无向同构和精确 top-k 搜索，仍为现有有向连接与 beam search。新回调接口直接传入关系与已绑定实体，避免从拼接文本反解析。40 项测试通过、3 项旧产物依赖测试跳过；尚无新 QA 分数。
- 后续 QA 预检 `6512188` 已提交，依赖 `6512008` 成功。三项对照为整条三元组评分、关系评分和 BM25，复用相同问题、出处上限与原文，均在同一次 Gemma/L40S 运行中重新 QA，不使用重排器。依然没有匹配就返回空证据，不从样本中删除失败题。输出分别为 `optimization_relation_scoring_pilot_seed42_20260919` 和 `optimization_relation_scoring_qa_seed42_20260919`，运行脚本只放 scratch。此时仅观察到部分候选改变，不据“Maria 捐车”等正面案例宣称 QA 改进；演员与同名电影的事实混淆也仍存在。
- 逐题原文检查发现，部分时间问题选中了同一个人的其他月份事实；日期在原文中并未丢失，但没有参与目前的连接与评分。关系评分只能检验谓词匹配，不自动解决时间约束。另有已知实体表述与原图字符串不一致导致空匹配，不能用新增任务词典或手写例外补齐。
- 已提交冻结事实候选的真实 QA 预检 `6511851`：复用 `6511739` 的 30 题和前五个匹配，不重新解析、不新增抽取；分别比较原排序、对整组事实重排、对其出处原文重排。独立事实候选与带实体约束的候选均使用相同 Qwen3-Reranker-0.6B；BM25 也有原顺序和相同模型重排对照。全部使用本地 Gemma3-4B、相同硬件、原生 QA 提示与评分；没有完整匹配时保留空证据，不添加 fallback。两候选产生方式仍有归一化等差异，不能将其比较解释为纯连接消融。完整协议在 `optimization_fact_evidence_pilot_seed42_20260919`，运行脚本只放 scratch，完成后归档。
- 固定 cap=3 的原文重排预检 `6511321` 因 `gpu` 配额等待，已迁至 `gpu-he` 并实际启动，仍使用 L40S，模型与协议未改。事实候选预检 `6511851` 最近状态为 `PENDING / QOSMaxGRESPerUser`，等待已有 GPU 作业释放资源，没有重复提交。
- `6511321` 随后成功完成，耗时 4 分 18 秒；官方示例的正负相关性顺序正确，批量与逐项评分差异小于 0.01。六任务 30 题、两后端四处理的 QA 均通过原生逐题核对。20 题 LoCoMo 上图证据只重排从 F1 31.80 到 38.20，但两题 2Wiki 上图证据重排截前六条从 75 到 25；样本不足以选择赢家。全量固定轨迹对照 `6511907` 已提交，仍为 Gemma、L40S、六任务各自全部问题、两后端四处理，不重跑 IRCoT 或构图。当前诊断不扩充旧图参数。
- `6511851` 已实际启动，并完成六任务全部候选准备。检查发现通用重排器仍会给不含目标关系的事实组合接近 1 的相关性分数。因此不把重排分数当成蕴含概率或证据完整性保证，也不针对这些例子设置概率阈值。事实重排和原文重排的 QA 仍需比较；在验证收益前不增加谓词别名规则。
- `6511851` 随后成功完成（2 分 31 秒），八种处理均完成原生 QA。20 题 LoCoMo 上，BM25 为 F1 23.52，独立事实原排序 19.19，实体约束原排序 17.12，实体约束加两种重排均为 19.35；SH/2Wiki 各两题有正面样例，但不足以支持统一方法的改进。完整八项六任务样例表见 `results.md`。暂不扩展该事实连接候选为全量；保留代码与失败证据，等待全量固定原文对照判断是否继续关系匹配设计，不能用微小样例的获胜任务替代四 reader、六任务目标。
- 查询模式首轮试验 `6511524` 已完成：六任务 15 组各前两题，共 30 题；使用问题文本生成关系模式，复用原 Qwen3-Embedding-0.6B 事实向量，每个关系独立取前 50 个事实后连接。19 题存在匹配、9 题无匹配、2 题格式无效。这不是正确率：检查发现已知人名被改成变量、比较操作被当成事实关系、主体/客体反向等问题；一个重复关系模式产生 68996 个匹配，也不能说明找对了证据。该候选不直接进入全量 QA。输出与运行代码保留在 `optimization_fact_join_pilot_seed42_20260919`。
- 下一轮 `6511739` 使用统一改写的提示，要求保留已知实体、只对未知值使用变量，比较等操作留给 reader。相同模式下比较独立事实检索与先约束已知主体/客体的 beam search，后者分别测严格字符串匹配和已有 HippoRAG 文本归一化。没有题目/任务分支，不读取答案。实现为 `optimization/retriever/query_pattern.py` 与 `fact_join.py`；beam search 和贪心集合覆盖均为常规算法，不作为新理论或 novelty。候选与打分输入也随搜索变化，不能把两者分差只归因于连接顺序。
- 本轮仍为 30 题的接口与失败诊断，不是六任务全量结果；beam width=8、出处上限=6 是未调优的试验设置，不声称必要或最优。没有完整事实匹配时明确记录失败，不用答案补全。产物位于 `optimization_bound_fact_join_pilot_seed42_20260919`，新提示不复用旧提示的解析缓存；原始三元组和向量只读。
- `6511739` 的单元检查通过 40 项、跳过 3 项旧产物依赖测试。运行中已出现关系语义不匹配：国籍问题的最佳匹配为两人的出生日期，说明约束已知实体仍不能保证谓词正确。该诊断只说明具体失败，不将部分样例推广成全量效果结论，也不据匹配数量增加宣称改进。
- `6511739` 已完成全部 30 题：相同新提示下，独立检索为 21 题匹配、8 题无匹配、1 题格式无效；严格字符串约束的 beam search 同为 21/8/1，使用已有文本归一化后为 23/6/1。匹配数量并非 QA 指标，严格约束没有在此数量上超过独立检索，额外两题不能归因于 beam search。尚无候选 QA 结果，不进入全量主实验。下一步应先测试已有语义相关性模型能否区分所需关系与同名实体的其他事实，并对逐条检索给相同评分模型；如果收益只来自评分模型，不归因于连接算法。试跑脚本完成后归档，不保留在活动目录。
- 已核对旧实现与缓存：存在原始三元组及出处，可先复用；旧图传播不直接读取谓词和主体/客体角色，但候选三元组匹配仍包含谓词，不能说 HippoRAG 整条流程都丢掉关系。
- 失败诊断：CPU 作业 `6511179` 已成功，耗时 28 秒。两模型、六任务、两后端、四个轮数的 54176 条预测全部重新计算原生分数，核对原始题目、原文列表和任务汇总。
- 结果位于 `optimization_ircot_batched_seed42_20260918/failure_analysis.json`，可复现脚本归档为同目录的 `failure_analysis_code.zip`，从活动脚本目录删除，不加入 git。不修改旧预测或轨迹。
- 索引基础已实现：`optimization/graph_construction/fact_index.py` 使用 SQLite 保存原始三元组、全部出现位置与出处，支持在候选事实之间执行主体/客体变量连接。同一事实的多个出处保留为替代来源，多条共同匹配的事实保留为不同依赖；不运行 PPR，不根据出现顺序删除值，不执行关系归一。字符串严格相等不解决别名，也不证明语义蕴含。自动查询解析、语义匹配和最终选择尚未接通，不是完整新算法。
- CPU 作业 `6511351` 成功：六任务 15 组、14362 个原文、按组去重后共 190667 个三元组、199351 条出现记录全部落盘，并从索引精确重建原始三元组出现序列。该检查针对三元组和出处，不声称复原 OpenIE 的所有辅助字段或证明抽取正确。38 项单元测试通过，3 项旧实验产物依赖测试跳过。产物为 `optimization_fact_index_seed42_20260919`，构建脚本归档为 `construction_code.zip` 并从活动目录删除。
- 固定候选对照已实现：`optimization/retriever/reranker.py` 按 [Qwen3-Reranker-0.6B 官方说明](https://huggingface.co/Qwen/Qwen3-Reranker-0.6B/blob/e61197ed45024b0ed8a2d74b80b4d909f1255473/README.md) 使用固定模型版本、通用指令和 yes/no 分数。不按任务改提示词；超过评分长度上限则报错，不静默截断。
- Gemma 试跑 `6511321` 已提交，最近调度状态为等待用户 GPU 配额。试跑覆盖六任务，每个原来源组前两题；通过后再跑全量并扩展 Qwen4B。固定 cap=3 的已有 BM25/图轨迹，分别比较 `original_all`、`rerank_all`、`original_top6`、`rerank_top6`，两后端都使用同一 reranker。原顺序对照也在同一 GPU 上重新 QA，避免将不同硬件运行差异误作排序收益。
- 本轮只是冻结轨迹上的最终 QA 控制，不代表改变了 IRCoT 中间检索，也不是新算法主结果。产物目录 `optimization_evidence_selection_seed42_20260919`，每次新增评分记录成本，已有 query/document 分数可复用；保持原生 loader、prompt、评分和生成设置。调度、缓存及脚本放在 scratch，完成后归档。
