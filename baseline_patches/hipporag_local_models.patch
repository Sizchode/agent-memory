diff --git a/pyproject.toml b/pyproject.toml
index b1acc3c..01c2e9a 100644
--- a/pyproject.toml
+++ b/pyproject.toml
@@ -10,7 +10,13 @@ readme = "README.md"
 requires-python = ">=3.10"
 dependencies = [
     "torch==2.5.1",
-    "transformers==4.45.2",
+    # Qwen3-Embedding requires Transformers >=4.51.0 according to its released
+    # model card. Keep the smallest compatible version for this experiment.
+    "transformers==4.51.0",
+    # Transformers' PyTorch weight loader imports init_empty_weights from
+    # Accelerate.  Declare the runtime dependency explicitly for local
+    # transformer embeddings instead of relying on an ambient environment.
+    "accelerate>=0.26.0",
     "openai>=3.3.1,<4",
     "litellm==1.73.1",
     "networkx==3.4.2",
diff --git a/src/hipporag/prompts/templates/ner.py b/src/hipporag/prompts/templates/ner.py
index 332e90c..cc23a76 100644
--- a/src/hipporag/prompts/templates/ner.py
+++ b/src/hipporag/prompts/templates/ner.py
@@ -1,5 +1,5 @@
 ner_system = """Your task is to extract named entities from the given paragraph. 
-Respond with a JSON list of entities.
+Respond with exactly one JSON object whose "named_entities" field is a list of entity strings.
 """
 
 one_shot_ner_paragraph = """Radio City
@@ -19,4 +19,4 @@ prompt_template = [
     {"role": "user", "content": one_shot_ner_paragraph},
     {"role": "assistant", "content": one_shot_ner_output},
     {"role": "user", "content": "${passage}"}
-]
\ No newline at end of file
+]
diff --git a/src/hipporag/prompts/templates/triple_extraction.py b/src/hipporag/prompts/templates/triple_extraction.py
index 06d6754..befedca 100644
--- a/src/hipporag/prompts/templates/triple_extraction.py
+++ b/src/hipporag/prompts/templates/triple_extraction.py
@@ -2,7 +2,7 @@ from .ner import one_shot_ner_paragraph, one_shot_ner_output
 from ...utils.llm_utils import convert_format_to_template
 
 ner_conditioned_re_system = """Your task is to construct an RDF (Resource Description Framework) graph from the given passages and named entity lists. 
-Respond with a JSON list of triples, with each triple representing a relationship in the RDF graph. 
+Respond with exactly one JSON object whose "triples" field is a list of triples, with each triple representing a relationship in the RDF graph. 
 
 Pay attention to the following requirements:
 - Each triple should contain at least one, but preferably two, of the named entities in the list for each passage.
@@ -47,4 +47,4 @@ prompt_template = [
     {"role": "user", "content": ner_conditioned_re_input},
     {"role": "assistant", "content": ner_conditioned_re_output},
     {"role": "user", "content": convert_format_to_template(original_string=ner_conditioned_re_frame, placeholder_mapping=None, static_values=None)}
-]
\ No newline at end of file
+]
