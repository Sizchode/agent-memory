# 第二轮探索记录：已停止，不再以 ensemble 为主方案

## 停止状态（2026-09-12 UTC）

用户指出这批方法主要拼接多个 baseline 的检索结果，不符合所需的单方法可插拔模块或独立完整 memory 算法。已取消 `6265880` 全部剩余子任务、`6265881` QA 后继、`6265924` 并行度调整任务，以及 `6265953` 检查笔记读出数组；取消后 `squeue` 为空。没有删除或覆盖 memory、完整结果、部分输出或日志。

新增 84 个计划评测单元中只有 16 个完整完成，其余为中断或未启动，不是完整六任务 × 三模型结论。`6265953` 的 36 个笔记读出评测未启动。旧有六个合并＋检查 9B 完成单元仍属于第一轮，不计为本轮新增。

下一步转为 [Generator 与 memory 产物检查](generator_memory_quality_review.md)，不继续 ensemble 扫描。以下保留原计划、对照与已完成的结果供分析，不代表当前在运行。

## 目标与判断方式

用户要求持续改进，争取所有 benchmark 和 evaluator 组合领先，至少在多数任务和模型上领先。采用一个固定版本跨任务评测，不按任务名称、题号、标准答案或已知分数选择不同方法或输出。训练自由，不改变模型权重；本轮不重建原始 memory。

当前可直接检验的是六个完整任务 × Qwen3.5-9B、4B、2B，共 18 个组合。比较对象包括 BM25、Dense、HippoRAG 2、指定 Mem0 SDK、LightMem 离线整合前后、AnchorMem 五条和官方设置。对原始 baseline 的版本与 adapter 限制沿用既有文档，不把本地领先等同于超过文献中所有不同模型和预算的结果。

仍使用 seed 42、non-thinking、原 benchmark 最终回答上限和确定性评分。官方示例和 LoCoMo 原选择题协议保持不变。研究过程中已看过这些任务，因此如实报告为 benchmark-guided development，不称作未见测试集泛化；没有增加答案注入或测试题专用规则。

## 第二轮开始时的完整结果

下表是同一个“合并证据＋一跳图连接最多追加五条”版本，与每个组合中的最高 baseline 分数比较。表中为百分制，四个 MemoryAgentBench 任务为 Substring Exact Match，LoCoMo 和 2Wiki 为对应官方 F1；不同指标不合成新总分。

| 任务 | 9B 最高 baseline | 9B 图连接 | 4B 最高 baseline | 4B 图连接 |
| --- | ---: | ---: | ---: | ---: |
| 单跳文档问答 | 88 | 94 | 89 | 90 |
| 多跳文档问答 | 59 | 66 | 54 | 62 |
| 单跳事实更新 | 66 | 75 | 56 | 57 |
| 多跳事实更新 | 6 | 5 | 5 | 8 |
| LoCoMo | 55.37 | 59.40 | 50.34 | 53.88 |
| 2Wiki | 51.87 | 58.04 | 49.76 | 55.53 |

这意味着已完成的 12 个组合中有 11 个点估计领先，不表示 11 个差距都具有统计显著性。2B 尚未完成；也不能将其他尝试在多跳事实更新中的 17% 替换进这个固定版本。合并版本依赖三套原始构建，部分输入更多，必须保留成本差异。

## 本轮操作与对照

复用第一轮实际有效但互补的两个操作：关系连接和回答前的证据检查。证据检查完全使用 `evaluate_memory_reasoning.py` 已有的 check prompt、最多 128 新 token、temperature 0；最终 QA 继续原协议。这是明确增加一次读出调用的组合，不是新增 memory 写入算法。

四个输入版本各自使用同一检查步骤：

1. HippoRAG 原取回内容：检查增加一次调用本身的收益。
2. 合并证据：已有的无图对照。
3. 合并证据＋一跳图追加：保留简单、已跨模型有效的连接。
4. 路径展示＋真实原文：检查较强的多跳事实更新版本能否补上对话拒答短板。

原计划覆盖六任务、三个 evaluator。合并证据＋检查的六个 9B 结果已完整完成，直接引用原结果，不重跑。原计划新增 66 个带检查的组合，另补合并／一跳图／路径原文的 18 个 2B 直接回答对照，共 84 个完整任务评测；取消时只完成其中 16 个。每个操作在所有任务上规则相同，没有根据结果动态选择分支。

调用 tokens、用时、原 prompt、模型、seed、源检索文件分别保存。原生成成本不计入增量读出日志，但最终成本比较必须加回原方法构建／检索成本。检查笔记标记为未经验证，不作为新增事实写回持久化 memory。

## 执行记录

- 新入口：`experiments/evaluate_memory_combinations.py`，复用既有回答 wrapper 和 scorer。
- Slurm：`6265880`，18 个 task/model 子作业，开始时最多并行五张 B200；连同第一轮剩余作业最多六张卡。
- 每张 GPU 并行三或四个独立读出进程；每个进程单独初始化 seed 42。时间申请四小时，避免照搬第一轮三小时造成尾部中断；这不是完成时间承诺。
- 日志：`/oscar/home/zliu328/memory_graph_check_6265880_*.out`。
- 结果：`/oscar/scratch/zliu328/agent-memory-outputs/memory_graph_check_seed42_20260912/<model>/<variant>/<task>/`。
- 没有更改原 baseline、数据转换、评分、Generator 或旧结果，没有创建测试文件。

第一轮最后的紧凑搜索 LoCoMo 已完成全部 1,986 题的搜索，原 QA 因三小时时限中断。后继 `6265881` 复用完整检索从 seed 42 重做 QA，没有重复搜索或构建 memory；随后也按用户要求取消。因此 `frontier_locomo_qa_completion_seed42_20260912` 下的预测仍是部分输出，不存在该分支的完整 LoCoMo 成绩。原查询成本与部分运行记录继续保留，不拼接两次随机轨迹。

## 已完成结果提供的诊断线索

9B 单跳事实更新的图连接＋检查为 68%，低于不加检查的 75%。逐题有 4 题提高、11 题降低；已检查的退步例子中，检查笔记给出任务支持的答案，最终 QA 却改回旧事实或因长句用尽原回答上限。这些是读取阶段的现象，不能解释为 Generator 构建错误。

为检查原证据干扰，曾编写并排队 `evaluate_compiled_evidence.py`：复用同一份检查笔记，最终 QA 只读笔记而不再读原证据，仍保留原 benchmark prompt 和预算。该候选可能省输入，也可能遗漏或放大错误；已在启动前取消，没有结果，不是已验证 memory 压缩方法。

不继续用 ensemble 或额外回答检查的成绩代替 memory 生产分析。下一次决策由原文到持久化 memory 的实际差异决定；条件保留、关系兼容和建图仍是假设。
